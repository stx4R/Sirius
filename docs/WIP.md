# WIP — 작업 재개 노트

> 이 파일은 **작업 인계용 메모**다. 사양의 정본은 `docs/GDD.md`이고(CLAUDE.md §9),
> 여기 적힌 것은 "아직 안 한 일"의 목록일 뿐이다. 단계가 끝나면 이 파일을 갱신한다.

**마지막 갱신**: 2026-08-06 · 커밋 **`P3-C`**(애니메이션 단계) 직후
**재개 방법**: "STA-mble 작업 중단된 곳부터 시작해줘"

---

## 1. 지금 상태

빌드는 전부 초록이다. `npx tsc --noEmit` 클린, `npx vitest run` **145개 통과**, `npm run dev` 정상.
1366×768에서 **오버플로 0** (실측), 2턴 연속 플레이 정상, 콘솔 에러 0건.

### 이번 세션에 끝낸 것 — 애니메이션 전면 도입 + 레이아웃 재구성

| 항목 | 내용 |
|---|---|
| **§9 정합성 점검** | GDD 본문 무결. 절 번호·상호참조·config 대조 전건 일치 |
| **core `cells`** | `SuitBreakdown.cells` 추가 — 정산 하이라이트 대상 칸. UI의 역추정 제거 |
| **상태 텍스트** | 5종 전부. `HUD.tsx`의 `Status` 유니온 |
| **드로우 애니메이션** | 좌측 밖 → 부채꼴 착지, stagger 60ms, spring 오버슈트 |
| **손패 아치** | 8장 기준 재설계. 폭 432px, 호버 시 좌우 벌어짐 + 떠오름 |
| **배치 이동** | `layoutId`로 손패→보드 실제 비행. 착지 바운스는 `LAND_SPRING`(damping 15) |
| **자물쇠** | `LOCK_GLYPH` 12×12 신설 + 칩 우상단 배지 팝인 |
| **디밍** | 셔플·정산 대기 중 미점등 칩 `brightness(0.4)` |
| **정산 애니메이션** | 문양 순서대로 600ms/문양, 골드 링 + 카드 발광 + 카운트업. 1×/2×/즉시 + 건너뛰기 |
| **정산 패널** | 문양 5종 컬럼 + 합산식 + 큰 노란 누적. 전부 `bySuit` 기반 |
| **ORION** | `ui/dialogue.ts` 7종 상황 × 3~5대사, 말풍선. 스프라이트는 P4 |
| **레이아웃** | 상단바 / 보드·별자리·정산 / 손패·ORION 3단. 중앙 정렬 실측 확인 |
| **reduced-motion** | `usePrefersReducedMotion` — 비행·stagger 0, 정산 단계 진행은 유지 |

### 확정된 결정 (다시 묻지 말 것)

1. **core breakdown = 승인됨.** `cells`까지 반영 완료.
2. **"전체 배율" 칸은 넣지 않는다.** GDD 5-2는 배율을 라인 단위로만 스택한다.
3. **문양별 "×배율"도 하나로 합치지 않는다.** 같은 이유다 — 한 문양에 ×2.5 라인과
   ×1.2 라인이 동시에 있으면 대표값이 존재하지 않는다. **발동한 라인의 배율을 그대로 나열**한다.
4. **`ORION'S WAGER` 명칭 유지.** **иєвυℓα 표기 그대로 노출.**
5. **ORION 대사 RNG는 `game.rng`와 분리한다.** `mulberry32(seed ^ 0x5ee0)`.
   게임 RNG를 쓰면 대사 뽑기가 덱·떠돌이 굴림을 소비해 같은 시드가 같은 판을 재현하지 못한다.
6. **`dialogue.ts`는 `src/ui/`에 둔다** (UI 한국어 텍스트). 따라서 **store는 이걸 import하지 않는다** —
   store→ui는 역방향이다. store는 `seed`만 노출하고, `ui/Orion.tsx`의 `useOrion`이 RNG를 쥔다.

---

## 2. 남은 작업

### 2-1. 커밋 라벨 — **`P3-C`로 확정**

`fac7826`이 이미 P3-B였으므로 이 단계는 **P3-C**다. P3-A → P3-B → P3-C로 이어진다.

### 2-2. 코드 주석 3곳 — **정리 완료**

GDD 본문은 처음부터 정상이었고, 어긋난 것은 코드 쪽 주석뿐이었다. 지시받아 전부 고쳤다.

| 위치 | 조치 |
|---|---|
| `src/assets/pixels.ts:5` | 별자리 카드 `32×48` → **36×52** (GDD 11-5) |
| `src/core/config.ts` | "never raise them" → GDD 12-4의 개정 취지(개별 수치가 아니라 **클리어율**)로 다시 씀 |
| `tests/assets.test.ts` | 테스트명 `32×48` → **36×52** |

`docs/GDD.md:650`의 `32×48 → 36×52` 서술은 **변경 이력**이므로 그대로 둔다.

### 2-3. GDD 경미 사항 (지시 없음)

- **8-3 DRIFT ORACLE**이 `₄C₃ = 4가지`만 서술한다. 3-3에 따르면 인접 3개 이하는 강제 선택이라
  경우가 1가지다 → P5 문항 설계 시 걸릴 공백
- `7-1-b`는 유일한 `N-M-알파벳` 형식, `6-0`은 유일하게 0에서 시작. 둘 다 의도로 보여 손대지 않음
- **11-8 표정 4종 중 '거래'** — 딜러였던 시절의 잔재. 대사 시스템이 붙었으니 P4에서 재검토

### 2-4. 이번 단계에서 드러난 것 (신규)

- **헤더가 정산 중에 다음 턴 번호를 보여준다.** `endTurn`이 turn을 먼저 올리기 때문에
  턴 1을 정산하는 동안 "턴 2"가 뜬다. P3-B부터 있던 동작이고 이번 범위 밖이라 손대지 않았다.
- 팔레트는 여전히 32색이다. 새로 쓴 색은 전부 `mix()` 파생 톤(보드 셀 테두리)이거나
  기존 색 재사용(`SUIT_INK` — ACR만 `acruxEdge`, GDD 11-7의 Acrux 경고 때문)이다.

---

## 3. 이 단계 이후

`P4` 동반성 30종 → `COMPANION_PARAMS`, `COMPANIONS_ENABLED` 플립, 티어 프레임 도트,
иєвυℓα 스프라이트·상점 화면, **ORION 48×64 스프라이트 + 표정 4종**, `Loadout.companions` 필수 전환(#15),
P2-B 재측정(#14).

иєвυℓα 팔레트는 **파생 톤(`mix()`)이 32색을 지키는 유일한 길**이다 — 보라·자주 후보가 전부 사용 중이고
동반성 티어 프레임 5색은 건드리지 않았다 (GDD 11-9 경고 블록, 미해결 #22).

그 다음 `P5` 학습 레이어(8절). DRIFT ORACLE은 미해결 #13(칩별 기여 분해)이 선행 조건인데,
**`bySuit`가 문양 단위까지 해결했고 `cells`로 칸 목록까지 나왔다.** 남은 것은 칸별 점수 기여다.
