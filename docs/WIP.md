# WIP — 작업 재개 노트

> 이 파일은 **작업 인계용 메모**다. 사양의 정본은 `docs/GDD.md`이고(CLAUDE.md §9),
> 여기 적힌 것은 "아직 안 한 일"의 목록일 뿐이다. 단계가 끝나면 이 파일을 갱신한다.

**마지막 갱신**: 2026-08-06 · **P4-A 진행 중 (토큰 소진으로 중단)**
**재개 방법**: "STA-mble 작업 중단된 곳부터 시작해줘"

---

## 0. ★ 재개하면 여기부터 — P4-A는 절반 끝났다

**끝난 것**: иєвυℓα 색·스프라이트·대사, 동반성 플래그 분리, GDD 갱신
**안 끝난 것**: **상점 화면 자체를 아직 안 만들었다.** 아래 §3이 남은 작업 전부다.

빌드는 초록이다. `npx tsc --noEmit` 클린, `npx vitest run` **164개 통과**.
게임 화면은 P3-D 상태 그대로 정상 동작한다. 상점만 없다.

---

## 1. 지금 상태

### 레이아웃은 고정 1120×630 캔버스다 (GDD 11-10). 유동으로 되돌리지 말 것

전 요소 절대 좌표 + `transform: scale()`. 배율은 **정수 내림**(CLAUDE.md §7).
좌표 정본은 `src/ui/Canvas.tsx`의 `LAYOUT`, GDD 11-10 표가 사본이다.
1366×768 → 1배, 2560×1440 → 2배 (실측 확인됨).

### P4-A에서 이번에 끝낸 것

| 항목 | 내용 |
|---|---|
| **§9 정합성 점검** | 절 번호·상호참조 정상. **불일치 1건 발견 → 승인받아 제거**(아래) |
| **иєвυℓα 팔레트** | `palette.ts`의 `NEBULA_INK` · `NEBULA_GLOW`. 전부 `mix()` 파생 톤 — 32색 유지 |
| **иєвυℓα 스프라이트** | `pixels.ts`의 `nebulaLayers()` + `compose.ts`의 `nebulaSprite(mood)`. 60×78 |
| **иєвυℓα 대사** | `ui/dialogue.ts`에 `NEBULA_LINES` · `shopLineFor`. 5상황 × 4~5개 |
| **동반성 플래그 분리** | `COMPANIONS_STOCKED`(진열, true) / `COMPANIONS_ENABLED`(구매, false) |
| **`SHOP_SLOTS.drifterChips` 제거** | GDD 13-4 위반 설정값. 지시받아 제거 |
| **GDD 갱신** | 9-3 떠돌이 진열 줄 삭제 · 13-4 보강 · 11-9 확정 내용으로 재작성 · 미해결 #22 종료, #23 신설 |
| **테스트 +19** | иєвυℓα 스프라이트 4건, 동반성 봉인 4건 포함 |

### иєвυℓα 스프라이트 — 다시 그리지 말 것, 계산해서 나온다

`nebulaLayers()`가 기하학으로 만든다. 손으로 그린 마스크가 아니다.
후드 돔 → **얼굴 자리 구멍(빛만)** → 어깨 → 로브 → 아래로 흩어져 소멸.

ASCII로 확인하려면:
```
npx tsx .tmp/preview.ts     # .tmp/는 gitignore 대상이 아니니 필요하면 지울 것
```

**두 번 실패했던 부분이니 되돌리지 말 것:**
- 밑단을 **픽셀 단위 난수**로 흩으면 잡티가 되어 실루엣이 통째로 깨진다
- 열 단위 난수도 1px 가시가 서서 안 된다
- → **저주파 사인파 2개, `fromCentre` 기준 대칭.** 난수 자체를 안 쓴다

### 확정된 결정 (다시 묻지 말 것)

1. core breakdown(`bySuit` + `cells`) 승인됨.
2. **"전체 배율" 칸 없음.** GDD 5-2는 배율을 라인 단위로만 스택한다.
3. **문양별 "×배율"도 합치지 않는다.** 발동 라인 배율을 그대로 나열. — 사용자 승인 완료
4. `ORION'S WAGER` 명칭 유지. **иєвυℓα 표기 그대로 노출.**
5. **ORION 대사 RNG는 `game.rng`와 분리.** `mulberry32(seed ^ 0x5ee0)`.
   иєвυℓα 대사도 **반드시 별도 스트림**을 쓸 것 — 게임 RNG를 소비하면 시드 재현이 깨진다.
6. `dialogue.ts`는 `src/ui/`. **store는 import하지 않는다**(store→ui는 역방향).
7. 레이아웃은 고정 캔버스. 8. 배율은 정수 내림.
9. **상태 텍스트에 exit 애니메이션 금지.** `AnimatePresence mode="wait"`는 프레임이 밀리면
   새 요소를 아예 마운트하지 않아 줄이 통째로 사라진다. 실제로 재현했다.

### ★ 동반성 봉인 — P4-B 전까지 절대 풀지 말 것

P2-B의 목표 곡선은 **전부 "동반성 없음" 기준**이다(GDD 13-6). 하나라도 상태에 새면 무효가 된다.

**런타임 가드가 아니라 타입으로 막혀 있다**: `core/shop.ts`의 `Purchase` 유니온에
`companion` 변형이 **없다.** 동반성 구매는 값으로 표현조차 되지 않는다.
→ **P4-B는 이 변형을 추가하는 것에서 시작한다.** (GDD 미해결 #23)

`tests/shop.test.ts`의 `describe('companions are stocked but never owned (P4-A)')`가
8라운드 완주 중 `ownedCompanions`가 항상 빈 배열임을 검사한다.

---

## 2. §9 감사 결과 (P4-A 착수 시)

절 번호 0~13 연속, 11-1~11-10 및 13-1~13-6 순차, 상호참조 정상.

**불일치 1건 → 승인받아 조치 완료**: `SHOP_SLOTS.drifterChips`가 GDD 13-4
("상점 진열에서도 뺀다")와 반대되는 말을 하고 있었다. `rollStock`이 읽지 않아 무해했으나
죽은 설정값이 되살아나는 것이 최악의 부채라 제거했다.

**남겨둔 것 (지시: 이번에 건드리지 말 것)**
- **GDD 8-3**이 DRIFT ORACLE을 `₄C₃ = 4가지`로만 서술한다. 3-3에 따르면 인접 3개 이하는
  강제 선택이라 경우가 1가지다. → **P5 선행 조건. 미해결 항목으로 유지.**
- `7-1-b`의 `N-M-알파벳` 형식, `6-0`이 0에서 시작 — 둘 다 의도로 보임
- 11-8 표정 4종 중 '거래' — 딜러였던 시절의 잔재. P4-B에서 재검토

---

## 3. ★ 남은 작업 — P4-A 후반부

지시받은 사양 그대로다. **동반성 효과(P4-B)·학습 장치(P5)·사운드(P6)는 만들지 않는다.**

### 3-1. 상점 화면 (`src/ui/Shop.tsx` 신설)

**1120×630 고정 캔버스 위에 절대 좌표로** 올린다. `LAYOUT`에 상점 항목을 추가하고
**GDD 11-10 좌표표도 함께 갱신할 것.**

라운드 클리어 후 진입. GDD 9-3 구성:
- 특수 조각 4종 (10종 중 랜덤)
- 별자리 2종 (12종 중 랜덤, 보유분 제외 — `rollStock`이 이미 처리)
- **동반성 2종 — 진열만, 구매 비활성.** 잠금 표시를 넣을 것
  (지시: "곧 만나게 될 것" 류. 살 수 없는 걸 그냥 보여주면 버그로 오해한다)
- 기본 조각 추가(3) / 제거(4)
- 리롤 (기본 2, 사용마다 +1, 라운드 넘어가면 초기화 — `startRound`/`openShop`이 이미 0으로 리셋)
- **떠돌이는 진열하지 않는다.** 첫 방문 시 `grantDrifter`가 무상 지급 (GDD 13-4)

иєвυℓα를 한쪽에 크게(120×156) 배치하고 말풍선으로 반응.
`shopLineFor`의 5상황을 붙일 것: `enter` / `bought` / `reroll` / `broke` / `leave`.
**동반성 잠금을 짚는 대사가 아직 없다** — `NEBULA_LINES`에 키를 하나 더 추가할 것
(지시 예시: "그건 아직 내놓을 때가 아니야").

### 3-2. 별자리 교체 UI

보유 한도 4. 4개인 상태에서 구매하면 **버릴 카드를 고르게** 한다.
`applyPurchase`의 `replaces` 필드가 이미 이걸 받는다 — core 수정 불필요.
선택 화면에서 **각 카드의 조건과 배율이 보여야** 한다(`ConstellationCard`가 이미 강제).

### 3-3. 덱빌딩 UI

- 현재 덱 구성을 **문양별 장수**로 표시, 추가/제거 시 즉시 갱신
- BLACK-HOLE(인벤토리)에서 보유 조각·별자리 확인

**★ P5의 STAR-CHART(GDD 8-1)가 여기에 붙는다.** 지금 데이터 표시 구조를 잡아둘 것.
`game.ownedDeck`을 문양별로 집계하는 순수 함수를 하나 두면 STAR-CHART가 그대로 재사용한다.

### 3-4. 화면 전환

게임 ↔ 상점 전환에 애니메이션. Framer Motion.
현재 `dismissSettlement`가 `openShop`을 열자마자 `startRound`로 빠져나간다
(`store/gameStore.ts`) — **상점 화면이 생기면 여기서 멈춰 서야 한다.** 이 부분을 고쳐야 한다.

### 3-5. 제약 (재확인)

- 게임 규칙을 UI에 넣지 않는다. `core/shop.ts`·`core/game.ts` 호출
- **core 수정이 필요하면 먼저 보고**
- 밸런스 숫자는 `config.ts`에서만
- 완료 후 **1라운드 클리어 → 상점 → 2라운드 진입**까지 실제 플레이 확인

---

## 4. 이 단계 이후

`P4-B` 동반성 효과 → `Purchase`에 `companion` 변형 추가, `COMPANION_PARAMS`,
`COMPANIONS_ENABLED` true 전환, 티어 프레임 5색 도트, `Loadout.companions` 필수 전환(#15),
**P2-B 재측정(#14)** — 동반성이 들어가면 목표 곡선을 다시 재야 한다.
ORION 60×78 스프라이트 + 표정 4종도 여기서.

그 다음 `P5` 학습 레이어(8절). DRIFT ORACLE은 미해결 #13(칩별 기여 분해)이 선행 조건인데
`bySuit`가 문양 단위, `cells`가 칸 목록까지 해결했다. 남은 것은 **칸별 점수 기여**다.
