# STA-mble — 게임 설계 문서 (GDD) v1.0

> GBL 2026 · Math 부문 · 주제: **확률과 통계**
> 타깃: 중학생 / 형식: 부스 시연 / 아트: 도트 그래픽

---

## 0. 한 줄 요약

플레이어는 5×5 성도(星圖)에 **남십자자리의 별 조각**을 놓아 **황도 12궁**을 완성하고,
딜러 **иєвυℓα**와 거래하며 덱을 강화한다. 플레이 중에는 **ORION(M42)**이 곁에서 해설한다.
매 순간의 선택은 **확률 판단**이며, 게임은 그 판단이 옳았는지를 **통계로 되돌려준다.**

---

## 1. 학습 목표 (교과 연계)

> **근거 자료**: 고등학교 『확률과 통계』 교사용 교과서 — **2022 개정 교육과정**, 류희찬 외 9인, 137쪽

### 1-1. 교과서 단원 구조

| 대단원 | 중단원 | 교과서 쪽 |
|---|---|---|
| **Ⅰ. 경우의 수** | 1. 여러 가지 순열 / 2. 중복조합 / 3. 이항정리 | 11 / 18 / 24 |
| **Ⅱ. 확률** | 1. 확률의 뜻 / 2. 확률의 덧셈정리 / 3. 조건부확률 / 4. 사건의 독립과 종속 | 41 / 50 / 54 / 60 |
| **Ⅲ. 통계** | 1. 확률변수와 확률분포 / 2. 이산확률변수의 기댓값과 표준편차 / 3. 이항분포 / 4. 정규분포 / 5. 모집단과 표본 / 6. 모평균과 모비율의 추정 | 77 / 85 / 93 / 101 / 110 / 121 |

### 1-2. ★ 중학생의 선수학습 (교과서가 직접 명시한 '배운 내용')

교과서는 각 대단원 도입부에 중학교 연계 내용을 명시한다. **이 게임의 난이도 기준선이다.**

| 학년 | 이미 배운 내용 | 게임에서 활용 |
|---|---|---|
| **중1** | 대푯값 / **도수분포표와 상대도수** | CONSTELLATION LOG의 상대도수 그래프 |
| **중2** | **경우의 수와 확률** | STAR-CHART, ORION'S WAGER의 기반 |
| **중3** | 산포도 / 상자그림과 산점도 | 점수 분포 시각화 |

> 🎯 **설계 원칙 확정**
> 중학생은 이미 **경우의 수·확률(중2)** 과 **상대도수(중1)** 를 배웠다.
> 따라서 이 게임은 *모르는 것을 가르치는 게* 아니라,
> **이미 아는 것(상대도수·확률)을 고교 개념(통계적 확률·기댓값)으로 잇는 다리**여야 한다.
> 부스에서 학생이 "아, 중학교 때 배운 그거네!"라고 말하게 만드는 것이 목표.

### 1-3. 게임 시스템 ↔ 성취기준 매핑

| 게임 시스템 | 대응 단원 | 성취기준 요지 (요약) |
|---|---|---|
| 덱 50장에서 8장 드로우 | Ⅰ-1, 중2 경우의 수 | 경우를 체계적으로 세는 방법 |
| STAR-CHART 실시간 패널 | **Ⅱ-1 확률의 뜻** | 확률의 개념과 기본 성질 설명 |
| ORION'S WAGER (기초) | **Ⅱ-2 확률의 덧셈정리** | 여사건의 확률을 활용해 문제 해결 |
| ORION'S WAGER (심화) | **Ⅱ-3 조건부확률** | 조건이 주어졌을 때의 확률을 실생활과 연결 |
| 매 턴 독립 드로우 | **Ⅱ-4 사건의 독립과 종속** | 독립·종속을 판단 |
| **DRIFT ORACLE** | **Ⅲ-2 기댓값** | 이산확률변수의 기댓값(평균)을 구함 |
| 동반성 티어 확률표 | Ⅲ-1 확률변수와 확률분포 | 확률분포의 뜻을 설명 |
| 덱 반환 vs 비복원 성운 | **Ⅲ-5 모집단과 표본** | 복원추출·비복원추출, 표본추출 방법 |
| **CONSTELLATION LOG** | **Ⅱ-1 통계적 확률 + Ⅲ-3 큰수의법칙** | 시행을 충분히 반복하면 상대도수가 수학적 확률에 접근 |
| 덱 빌딩 (칩 추가/제거) | Ⅲ-6 모비율 | 표본비율과 모비율의 관계 |

### 1-4. 교과서 반영 필수 수정 사항 ⚠️

교과서를 정독한 결과 **기존 설계에서 반드시 고쳐야 할 3가지**:

**① 용어 표기**
교과서는 **`큰수의법칙`** 으로 붙여 쓴다(띄어쓰기 없음). 게임 내 모든 표기를 통일한다.
또한 이 개념은 확률 단원이 아니라 **Ⅲ-3 이항분포**의 학습 요소다.

**② `비복원추출`의 단원 위치**
조건부확률(Ⅱ-3)이 아니라 **Ⅲ-5 모집단과 표본**에 속한다.
따라서 STAR-CHART의 설명 문구는 "조건부확률"이 아니라 **"표본추출"** 맥락으로 쓴다.

**③ ★ 조건부확률 문제 설계 — 교과서의 명시적 경고**
교과서 지도상의 유의점은, 조건부확률을 **조건이 주어진 상황의 가능성**으로 이해시키되
**사건의 시간적 순서나 인과관계로 해석하지 않도록** 하라고 못박고 있다.

| | 문구 |
|---|---|
| ❌ **금지** | "Acrux가 **이미 3장 나갔다. 이제** Ginan이 더 나올까?" → 시간적 순서 유도 |
| ✅ **수정** | "**지금 덱이 이 상태일 때**, Ginan이 Acrux보다 많이 나올까?" → 조건 상태 제시 |

기존 8-2절의 라운드 6–8 예시 문구는 이 원칙에 따라 전량 재작성한다.

### 1-5. 게임 테마 문장

교과서 Ⅱ단원의 핵심 아이디어는 확률을 **합리적 의사 결정의 도구**로 규정한다.
이 문장이 STA-mble의 정체성과 정확히 일치하므로, **게임 타이틀 화면과 부스 배너의 태그라인**으로 사용한다.

> *"확률은 운을 예측하는 것이 아니라, 더 나은 선택을 하는 도구다."*

### 1-6. 보너스 소재 (선택)

교과서 Ⅱ단원 '미래 수학 유니버스'에 **몬티 홀 문제**가 실려 있다.
ORION의 특별 이벤트("세 개의 성운 중 하나에만 보상이 있다")로 재현하면
교과서와의 연계를 심사에서 직접 증명할 수 있다. → Phase 6에서 검토

---

## 2. 세계관 · 명명 규칙

| 게임 용어 | 실제 대응 | 비고 |
|---|---|---|
| 별자리 조각 | 남십자자리(Crux) α~ε별 | 칩 형태 |
| 별자리 | 황도 12궁 | 족보(役) 시스템 |
| ORION (코드네임 M42) | 오리온 대성운 | 플레이 해설자 · 동반자 (딜러 아님) |
| иєвυℓα | 실존 천체 아님 — 정체불명의 존재 | 상점 딜러 |
| BLACK-HOLE | 블랙홀 | 인벤토리 |
| 동반성 | 쌍성계의 동반성 | 패시브 |
| 스타더스트 | 성간 먼지 | 화폐 |

**톤 & 매너**: "Gamble"은 사행성이 아니라 **확률에 근거한 항해적 판단**으로 연출한다.
카지노 이미지(현금·베팅액·잭팟)는 사용하지 않는다. → 학교 행사 심의 대응

---

## 3. 별자리 조각 (칩)

### 3-1. 기본 조각 5종

| ID | 이름 | 색 | 문양 | 기본 점수 |
|---|---|---|---|---|
| GAC | Gacrux | 연두 `#7FD44C` | 클로버 ♣ | 10 |
| IMA | Imai | 파랑 `#3F8FE0` | 다이아 ♦ | 10 |
| GIN | Ginan | 빨강 `#E0453F` | 하트 ♥ | 10 |
| MIM | Mimosa | 보라 `#9A4FD4` | X자 ✕ | 10 |
| ACR | Acrux | 검정 `#2B2B38` | 스페이드 ♠ | 10 |

**정산 순서(고정)**: Gacrux → Imai → Ginan → Mimosa → Acrux

### 3-2. 특수 별자리 조각 (₅C₂ = 10종)

두 문양을 반반 합친 칩. 정산 시 **두 문양 모두로 판정**되며, 양쪽 라인에 동시 기여한다.

**표기 규칙**: 좌/우는 **정산 순서(GAC → IMA → GIN → MIM → ACR)** 로 정규화한다.
즉 항상 순서가 앞선 문양이 `left`, 뒤선 문양이 `right`다. 중첩 루프 `i < j`로 10종이 자동 생성된다.

```
GAC&IMA  GAC&GIN  GAC&MIM  GAC&ACR
IMA&GIN  IMA&MIM  IMA&ACR
GIN&MIM  GIN&ACR
MIM&ACR
```

> 좌/우는 **시각적 배치에만** 영향을 준다(왼쪽 절반 색·문양). 정산은 두 문양을 대등하게 처리하므로
> 순서를 정규화해도 게임 로직에 변화가 없다. 초기 설계안 대비 3종의 좌우가 바뀐다
> (MIM&GAC → GAC&MIM, ACR&GAC → GAC&ACR, ACR&IMA → IMA&ACR).

**디자인 규칙**
- 칩 왼쪽 절반 = Something1의 색 / 오른쪽 절반 = Something2의 색
- 중앙 문양 = Something1 문양의 **왼쪽 절반** + Something2 문양의 **오른쪽 절반**을 이어 붙임

**획득**: иєвυℓα 상점에서만. 매 상점마다 10종 중 **랜덤 4종** 진열.

### 3-3. 떠돌이 별자리 조각 (1종, 게임당 1개 한정)

정산 시 인접 4방향 중 **랜덤 3방향**을 선택해, 그 방향에 있는 칩의 문양으로 **전부 판정**된다.

| 인접 칩 수 | 선택 방식 |
|---|---|
| 4개 | 랜덤 3방향 선택 (₄C₃ = 4가지, 각 1/4) |
| 3개 | 3개 전부 강제 선택 |
| 2개 | 2개 전부 강제 선택 |
| 1개 | 1개 강제 선택 |
| 0개 | 판정 없음 (0점) |

- 판정된 문양으로 **점수·라인 판정 모두** 참여한다.
- **중복 문양은 집합으로 합친다.** 선택된 3방향이 Gacrux·Gacrux·Ginan이면 결과는 {Gacrux, Ginan} 2종이다.
  떠돌이는 칸 하나를 차지하므로, 한 칸이 "Gacrux 2개"일 수는 없다. 라인 판정과 모순되지 않기 위한 필연적 규칙이다.
- **인접 칩이 특수 조각이면 두 문양을 모두 얻는다.** 특수 조각은 어디서든 두 문양 그 자체이므로 일관되게 처리한다.
  따라서 한 방향에서 2종을 얻을 수 있고, 3방향 전부 특수 조각이면 최대 5종(중복 제거 후)까지 가능하다.

> 🎯 **전술적 함의**: 집합 규칙 때문에 떠돌이는 **같은 문양이 몰린 곳보다 문양 경계에 놓을 때** 강하다.
> 별자리 시스템이 같은 문양 연속을 보상하는 것과 방향이 반대라, 배치 판단에 긴장이 생긴다.
- **지급 규칙**: 판매하지 않는다. **иєвυℓα와의 첫 만남에서 1개를 무상 지급**하고, 이후 다시 등장하지 않는다 (13-4절).

---

## 4. 보드 · 코어 루프

### 4-1. 구조

```
[게임 시작]
   └─ 라운드 1 ~ 8 반복
        ├─ 보드 초기화 (5×5 전부 비움) + 덱 전체 리셋 · 셔플
        ├─ 턴 1 ~ 5 반복
        │    ├─ ORION'S WAGER (예측 베팅) ── 선택제
        │    ├─ 덱에서 8장 드로우 (비복원)
        │    ├─ 손패 8장 중 최대 4장을 보드에 배치 → 즉시 고정
        │    ├─ 배치 안 한 조각은 덱으로 반환 후 셔플
        │    └─ 정산 → 라운드 누적 점수에 가산
        ├─ 누적 점수 ≥ 목표치 → 라운드 클리어 / 미달 → 게임 오버
        ├─ CONSTELLATION LOG (통계 리포트)
        └─ иєвυℓα 상점
```

### 4-2. 확정 규칙

| 항목 | 값 | 근거 ID |
|---|---|---|
| 보드 | 5×5 (25칸) | — |
| 턴당 드로우 | 8장 (비복원) | — |
| 턴당 배치 한도 | **최대 4장** (손패 8장 중 선택) | C-1 |
| 미배치 조각 | **덱 반환 후 셔플** | C-2 |
| 라운드당 최대 배치 | 20칸 (5칸 여백은 의도된 설계) | C-3 |
| 정산 시점 | **매 턴 정산 → 라운드 내 누적** | C-4 |
| 총 라운드 | **8라운드** (40턴, 약 40분) | 확정 |
| 실패 조건 | 5턴 후 목표 미달 시 즉시 게임 오버 | C-5 |
| 라운드 간 리셋 | 보드는 비우고, 덱은 **보유 덱**(상점 구매분 반영)으로 되돌린 뒤 셔플 | 명확화 |
| 초기 덱 | **5종 × 10장 = 50장** | C-7 |

---

## 5. 정산 알고리즘 (핵심)

### 5-1. 의사코드

```
총점 = 0
for 문양 in [GAC, IMA, GIN, MIM, ACR]:          # 고정 순서
    S = 보드 위에서 이 문양으로 판정되는 모든 칩
    if S 비어있음: continue

    발동라인들 = 보유 별자리로 탐색한 maximal 라인 전체

    for 라인 L in 발동라인들:
        라인배율 = Σ( L 조건을 만족시키는 보유 별자리들의 배율 )
        총점 += (L의 칩 수 × 10) × 라인배율

    비라인칩 = S 중 어떤 발동라인에도 속하지 않는 칩
    총점 += 비라인칩 수 × 10 × 1.0

    if 게자리 발동 and 이 문양이 최다 문양:
        총점 += S 전체 수 × 10 × 0.1
```

### 5-2. 배율 규칙 (확정)

| 규칙 | 내용 | 근거 ID |
|---|---|---|
| 스택 방식 | **단순 합산** — 세로 5연속 시 1.2 + 1.8 + 2.5 = **×5.5** | B-1, B-2 |
| 적용 범위 | **라인에 포함된 칩에만** 적용 (문양 전체 아님) | B-3 |
| 교차 중복 | 한 칩이 가로·세로 라인에 동시 포함되면 **양쪽에서 각각 계산** | B-4 |
| 라인 인정 | **maximal run만** (5연속 안의 3연속 부분집합을 따로 세지 않음) | 신규 확정 |
| 도형 중복 | **꼭짓점이 다르면 칸을 공유해도 각각 별개 라인** (ㅅ자·ㅗ자 포함) | 신규 확정 |
| 대각선 정의 | ↘·↗ 양방향 + 오프셋 대각선 전부 인정 | B-5 |

> 🔧 **튜닝 포인트**: 스택 방식(단순합 vs 곱연산 vs `1.0 + Σ(배율-1.0)`)은
> `src/core/config.ts`의 상수로 분리해 몬테카를로로 비교 검증할 것.

### 5-3. 계산 예시

Gacrux 5개가 1열에 세로 연속 배치 + 양자리·염소자리·궁수자리 보유

```
라인배율 = 1.2 + 1.8 + 2.5 = 5.5
점수     = (5칸 × 10점) × 5.5 = 275점
```

---

## 6. 별자리 12종 (족보)

**보유 한도 4개** / 상점당 12종 중 **랜덤 2종** 진열 / 초과 시 기존 것을 버리고 교체

| # | 이름 | 축 | 조건 | 배율 |
|---|---|---|---|---|
| 1 | **궁수자리** | 세로 | 같은 문양 연속 5 | ×2.5 |
| 2 | **염소자리** | 세로 | 같은 문양 연속 4 이상 | ×1.8 |
| 3 | **물병자리** | ㅅ자 | 아래 패턴 정의 참조 | ×2.0 ~ 3.5 |
| 4 | **물고기자리** | 가로 | 같은 문양 연속 4 이상 | ×1.8 |
| 5 | **양자리** | 세로 | 같은 문양 연속 3 이상 | ×1.2 |
| 6 | **황소자리** | 대각 | 같은 문양 연속 4 이상 | ×2.1 |
| 7 | **쌍둥이자리** | 대각 | 같은 문양 연속 3 이상 | ×1.5 |
| 8 | **게자리** | 전역 | 보드 최다 배치 문양 | ×1.1 |
| 9 | **사자자리** | 가로 | 같은 문양 연속 5 | ×2.5 |
| 10 | **처녀자리** | ㅗ자 | 아래 패턴 정의 참조 | ×1.5 ~ 2.5 |
| 11 | **천칭자리** | 가로 | 같은 문양 연속 3 이상 | ×1.2 |
| 12 | **전갈자리** | 대각 | 같은 문양 연속 5 | ×3.0 |

> **A-1 해소**: 원문의 '사수자리'와 '궁수자리'는 동일 별자리. → **궁수자리**로 통일

### 6-0. 배율 자료구조 (확정)

10종은 고정 배율, 2종(물병·처녀)은 패턴 크기에 따라 변동한다. 판별 유니온으로 통일한다.

```ts
type MultiplierSpec =
  | { kind: 'fixed';  value: number }
  | { kind: 'bySize'; table: Record<number, number> };   // key = 구성 칸 수
```

| 별자리 | 스펙 |
|---|---|
| 물병자리 | `bySize: { 3: 2.0, 5: 3.5 }` |
| 처녀자리 | `bySize: { 4: 1.5, 5: 1.9, 6: 2.2, 7: 2.5 }` |
| 나머지 10종 | `fixed: <표의 값>` |

`bySize`의 키는 서로 충돌하지 않으므로 칸 수만으로 배율이 유일하게 결정된다.
config는 **데이터만** 담는다 — 함수를 넣지 않는다(시뮬레이터가 값을 치환해야 하므로).

### 6-1. 'ㅅ'자 패턴 정의 (물병자리) — A-2 해소

꼭짓점 `(r, c)`에서 좌하 대각 n칸 + 우하 대각 n칸이 같은 문양으로 연속.

```
n = 1 (3칸)            n = 2 (5칸)
    ●                      ●
   ● ●                    ● ●
                         ●   ●
```

| n | 구성 칸 수 | 배율 | 5×5에서 가능한 위치 |
|---|---|---|---|
| 1 | 3 | ×2.0 | 다수 |
| 2 | 5 | ×3.5 | 꼭짓점이 (0,2)일 때만 |

### 6-2. 'ㅗ'자 패턴 정의 (처녀자리) — A-3 해소

가로 받침 m칸(연속) + 받침 **중앙**에서 위로 k칸.

| m (받침) | k (기둥) | 구성 칸 수 | 배율 |
|---|---|---|---|
| 3 | 1 | 4 | ×1.5 |
| 3 | 2 | 5 | ×1.9 |
| 5 | 1 | 6 | ×2.2 |
| 5 | 2 | 7 | ×2.5 |

### 6-3. 게자리 특례 — B-7 해소

- 보드에서 **가장 많이 배치된 문양**에 적용
- 동점 시 해당 문양 **전부** 발동
- 유일하게 **라인이 아닌 문양 전체**에 적용되는 별자리 (+10% 가산)

---

## 7. 동반성 시스템 (30종)

**보유 한도 2개** / 상점당 **랜덤 2종** 진열 / иєвυℓα에서만 획득

### 7-1. 티어 · 등장 확률

| 티어 | 이름 | 종수 | 등장 확률 | 가격 |
|---|---|---|---|---|
| Rare | 가스 | 10 | 45% | 6 |
| Super Rare | 성운 | 8 | 27% | 9 |
| Epic | 원시별 | 6 | 17% | 13 |
| Mythic | 주계열성 | 4 | 8% | 18 |
| Legendary | 초거성 | 2 | 3% | 25 |

> 📊 이 확률표는 **게임 내에 공개 표시**한다. (확률분포 학습 소재 + 확률형 아이템 표시 의무 대응)

### 7-1-b. 데이터 단계 분리 (확정)

동반성 효과의 수치(18점, 16점, +15점, 20% 등)는 **효과 시스템 설계에 종속**된다.
따라서 자료구조를 두 단계로 나눈다.

| 단계 | 파일 | 내용 |
|---|---|---|
| **P0** | `config.ts` → `COMPANIONS` | `id`, `name`, `tier`, `price`, `description`(한국어) |
| **P4** | `config.ts` → `COMPANION_PARAMS` | 효과 파라미터 수치 |

P0에서 효과 파라미터를 미리 만들지 않는다. 형태를 모르는 상태의 추상화는 반드시 틀린다.

### 7-2. 전체 목록

`📊` = 확률·통계 개념 직결 (10종)

#### ⭐ Legendary — 초거성 (2종)

| # | 이름 | 효과 |
|---|---|---|
| 1 | 클로버 초거성 | 보드 상하좌우 끝이 연결되어(토러스), 반대편 칩과도 별자리가 이어진다 |
| 2 | 📊 큰수의법칙 초거성 | 라운드가 끝날 때마다 모든 배율이 **+0.1 영구 누적**된다 |

#### 🌟 Mythic — 주계열성 (4종)

| # | 이름 | 효과 |
|---|---|---|
| 3 | 📊 기댓값 주계열성 | 떠돌이 조각이 항상 4가지 경우 중 **최고 점수**로 판정된다 |
| 4 | 맥동 주계열성 | 턴마다 점수 배율이 ×0.7 → ×1.8 순으로 번갈아 적용된다 |
| 5 | 초신성 주계열성 | 라운드 **마지막 턴** 점수가 ×2가 된다 |
| 6 | 쌍성 주계열성 | 동반성 슬롯이 1개 늘어난다 (최대 3개) |

#### 💫 Epic — 원시별 (6종)

| # | 이름 | 효과 |
|---|---|---|
| 7 | 오리 원시별 | 별자리가 발동될 때마다 무작위 칩의 가치가 상승한다 |
| 8 | 📊 여사건 원시별 | ORION'S WAGER를 틀려도 보상의 **절반**을 획득한다 |
| 9 | 📊 표본추출 원시별 | 매 턴 손패가 8장 → **10장**이 된다 |
| 10 | 📊 조건부 원시별 | 덱에 **가장 적게 남은 문양**의 칩 점수가 ×2가 된다 |
| 11 | 적색 원시별 | Ginan 칩의 기본 점수가 10 → **18**이 된다 |
| 12 | 암흑 원시별 | 배치하지 않은 조각 1장당 스타더스트 +1 |

#### ✨ Super Rare — 성운 (8종)

| # | 이름 | 효과 |
|---|---|---|
| 13 | 📊 표본평균 성운 | 최근 3턴 **평균 점수의 20%**를 매 턴 보너스로 받는다 |
| 14 | 📊 비복원 성운 | 배치한 칩이 덱으로 돌아오지 않는다 (덱 확률이 급격히 변한다) |
| 15 | 📊 독립시행 성운 | 매 턴 **20% 확률**로 그 턴 점수가 ×1.5가 된다 |
| 16 | 불안정 성운 | 매 턴 점수에 ×0.5 ~ ×2.0의 무작위 배율이 곱해진다 |
| 17 | 황도 성운 | 별자리 보유 한도가 4 → 5로 늘어난다 |
| 18 | 거울 성운 | 보드 좌우 대칭 위치에 같은 칩이 있으면 둘 다 +50% |
| 19 | 자기장 성운 | 같은 문양끼리 인접할 때마다 각각 +5점 |
| 20 | 적운 성운 | 특수 조각의 두 판정이 **모두** 라인에 포함되면 +30% |

#### 🔹 Rare — 가스 (10종)

| # | 이름 | 효과 |
|---|---|---|
| 21 | 📊 표본크기 가스 | 라운드마다 덱에 무작위 칩 2장이 추가된다 |
| 22 | 📊 상대도수 가스 | 이번 라운드에 가장 많이 배치한 문양이 개당 +15점 |
| 23 | 먼지 가스 | 모든 칩의 기본 점수 +2 |
| 24 | 청색 가스 | Imai 칩의 기본 점수가 10 → 16 |
| 25 | 녹색 가스 | Gacrux 칩의 기본 점수가 10 → 16 |
| 26 | 자색 가스 | Mimosa 칩의 기본 점수가 10 → 16 |
| 27 | 흑색 가스 | Acrux 칩의 기본 점수가 10 → 16 |
| 28 | 행성상 가스 | 보드 네 모서리에 배치된 칩이 ×1.5 |
| 29 | 중심핵 가스 | 보드 정중앙 칸의 칩이 ×2.5 |
| 30 | 혜성 가스 | 라운드 **첫 턴** 점수가 ×1.5 |

---

## 8. 확률·통계 학습 레이어 (B안)

### 8-1. STAR-CHART — 실시간 덱 확률 패널

BLACK-HOLE 옆 **상시 표시**.

| 표시 | 내용 |
|---|---|
| 문양별 잔여 장수 | Ginan 7 / 40 |
| 잔여 비율 | 17.5% |
| 다음 8장에 1장 이상 나올 확률 | `1 − C(33,8)/C(40,8)` = 78.4% |

드로우마다 막대 그래프가 실시간 변동 → **비복원추출**(Ⅲ-5 모집단과 표본)을 시각적으로 체감.

### 8-2. ORION'S WAGER — 예측 베팅 ★핵심

- **시점**: 매 턴 드로우 **직전**
- **형식**: `YES` / `NO` / `기권` 3지선다
- **보상**: 적중 시 스타더스트 +2, 오답 시 없음 (**패널티 없음**)
- **강제성**: 선택제. **단, 게임 최초 3회는 튜토리얼로 강제** (확정)

**난이도 곡선**

| 라운드 | 문제 유형 | 예시 | 교과 |
|---|---|---|---|
| 1–2 | 단순 비교 | "Ginan과 Acrux 중 어느 쪽이 더 많이 나올까?" | Ⅱ-1 수학적 확률 |
| 3–5 | 여사건 활용 | "이번 8장에 Ginan이 **1장도 안 나올** 확률이 절반을 넘을까?" | Ⅱ-2 여사건 |
| 6–8 | 조건부 | "**지금 덱이 이 상태일 때**, Ginan이 Acrux보다 많이 나올까?" | Ⅱ-3 조건부확률 |

> ⚠️ **문항 작성 절대 규칙 (교과서 지도상의 유의점)**
> 조건부확률 문항은 **"이미 ~했으니 이제"** 같은 **시간적 순서·인과관계 표현을 쓰지 않는다.**
> 반드시 **"지금 덱이 이러할 때"** 처럼 **조건 상태를 제시하는** 형태로만 작성한다.
> 교과서가 명시적으로 경고한 오개념이므로, 위반 시 교육적으로 역효과가 난다.

**오답 시 즉시 해설 팝업** — 왜 그 확률인지 1~2문장으로.

### 8-3. DRIFT ORACLE — 기댓값 계산

떠돌이 조각 배치 후 정산 **직전**, ₄C₃ = 4가지 경우를 표로 전부 공개.

| 선택된 3방향 | 판정 문양 | 점수 | 확률 |
|---|---|---|---|
| 상·하·좌 | ACR, MIM, GIN | 84 | 1/4 |
| 상·하·우 | ACR, MIM, GAC | 96 | 1/4 |
| 상·좌·우 | ACR, GIN, GAC | 72 | 1/4 |
| 하·좌·우 | MIM, GIN, GAC | 68 | 1/4 |

→ **"이 조각의 기댓값은?"** 3지선다 → 정답 시 그 턴 떠돌이 점수 **+50%**

#### 인접 칩 수별 동작 (3-3절과의 정합)

위 표는 **인접 칩이 4개일 때**만이다. 3-3절은 인접 칩이 3개 이하면 **강제 선택**이라고
규정한다 — 고를 것이 없으므로 경우의 수가 하나다.

| 인접 칩 수 | 경우의 수 | 각 확률 | 화면 |
|---|---|---|---|
| 4 | ₄C₃ = **4** | 1/4 | 위 표 그대로 |
| 3 | **1** (셋 다 선택) | 1 | 판정이 하나로 정해짐 |
| 2 | **1** | 1 | 〃 |
| 1 | **1** | 1 | 〃 |
| 0 | **1** (판정 문양 없음) | 1 | 〃 |

> **경우의 수가 1가지일 때 기댓값 문제를 출제할지는 P5 결정 사항으로 남긴다.**
> 확률 1의 사건에 기댓값을 묻는 것이 학습상 유효한지(자명해서 무의미한지, 오히려
> "확률 1"의 이해를 굳히는지)는 문항 설계와 함께 판단해야 한다. 지금 정하지 않는다.

### 8-4. CONSTELLATION LOG — 라운드 종료 통계 리포트

라운드 클리어 시 자동 표시.

1. **내 예측 적중률 vs 이론 확률** (막대 비교)
2. **문양별 실제 등장 횟수 vs 기댓값** (막대 비교)
3. 라운드가 쌓일수록 두 값이 수렴하는 애니메이션 → **큰수의법칙** (Ⅲ-3)

> 🎪 **Phase 8 확장**: 부스 참가자 전원의 데이터를 누적해 대형 모니터에 실시간 표시.
> 표본이 커질수록 이론값에 수렴하는 것을 관객 전체가 목격 → **투표 어필 최상급 요소**

---

## 9. 경제 (스타더스트)

### 9-1. 획득

| 경로 | 금액 |
|---|---|
| 라운드 클리어 | 5 + 라운드 번호 |
| 목표 초과 보너스 | 초과분 ÷ 100 (최대 5) |
| ORION'S WAGER 적중 | +2 |
| DRIFT ORACLE 정답 | +1 |

### 9-2. 지출

| 상품 | 가격 |
|---|---|
| 기본 조각 덱에 추가 | 3 |
| 기본 조각 덱에서 제거 | 4 |
| 특수 별자리 조각 | 8 |
| 떠돌이 별자리 조각 | **무상 지급** (게임당 1회, 판매하지 않음 — 13-4절) |
| 별자리 | 10 |
| 동반성 | 6 / 9 / 13 / 18 / 25 (티어별) |
| 상점 리롤 | 2 (사용할 때마다 +1) |

### 9-3. 상점 구성 (라운드 클리어 시)

```
┌────────── иєвυℓα ───────────────┐
│  특수 조각 4종  (10종 중 랜덤)   │
│  별자리     2종  (12종 중 랜덤)   │
│  동반성     2종  (티어 가중 랜덤) │
│  기본 조각  추가 / 제거          │
│  [ 리롤 ]                        │
└──────────────────────────────────┘
```

#### ★ 한 슬롯은 방문당 1회만 판매한다 (P4-A 확정)

특수 조각·별자리 슬롯은 **구매되면 진열에서 빠진다.** 다시 사려면 리롤해야 한다.
`core/game.ts`의 `buy`가 `core/shop.ts`의 `soldOut`을 통해 처리하며, **구매가 거절되면
(이미 보유한 별자리 등) 스타더스트를 쓰지 않았으므로 진열에도 그대로 남는다.**

> 이것이 UI가 아니라 core에 있는 이유: 13-6절의 목표 곡선은 각 진열 품목을 **최대 1회**
> 사는 상점 정책(`sim/player.ts`)으로 측정됐다. 슬롯이 무한히 다시 팔리면 스타더스트가
> 넉넉한 플레이어가 같은 특수 조각을 4장까지 살 수 있고, 그건 측정된 적 없는 덱이다.

**기본 조각 추가·제거는 슬롯이 아니다.** 9-3의 진열도에 상시 항목으로 올라 있으므로
몇 번을 써도 사라지지 않는다.

---

## 10. 밸런스 목표치

| 라운드 | 목표 누적 점수 | 성장 | 비고 |
|---|---|---|---|
| 1 | **490** | — | 튜토리얼 라운드 (전원 통과) |
| 2 | **630** | ×1.29 | |
| 3 | **640** | ×1.02 | ← **부스판 최종 라운드** |
| 4 | **880** | ×1.38 | |
| 5 | **1,080** | ×1.23 | |
| 6 | **1,250** | ×1.16 | |
| 7 | **1,330** | ×1.06 | |
| 8 | **1,530** | ×1.15 | |

**8라운드 총 성장 ×3.1.** 기존 곡선(×30)이 틀렸던 이유는 규칙상 복리 성장이 불가능하기 때문이다 —
보드는 5×5로 고정, 라운드당 배치 20칸 고정, 별자리 보유 한도 4개 고정.
성장할 수 있는 건 배율과 덱 품질뿐이고, 둘 다 상한이 있다.

### 10-1. 검증 결과 (P2-B, 조합당 1,000회 × 9조합)

최종 규칙(떠돌이 무상 지급 + 시작 별자리) 기준. 상세는 13-6절.

| 조건 | 측정값 | 판정 |
|---|---|---|
| smart 풀버전 클리어율 20~30% | **28.1%** | ✅ |
| greedy 부스판 클리어율 ≥70% | **85.3%** | ✅ |
| 전 라운드 ≤ 기계적 상한 70% | 최대 68.6% (R1) | ✅ |
| WAGER 0%에서도 greedy 부스판 ≥70% (12-5절) | **71.3%** | ✅ |

### 10-2. R1은 관문이 아니라 튜토리얼이다

R1의 동적 범위는 바닥 600 ~ 상한 714로 **19%뿐**이며, 목표 490은 전원 통과한다.
이는 의도된 것이다.

| 이유 | 내용 |
|---|---|
| 부스 온보딩 | 첫 5턴에서 탈락시키면 그 참여자의 표를 잃는다 |
| 피드백은 존재 | 시작 별자리 도입으로 표준편차 0 → **28**. 점수가 배치에 반응한다 |
| 개념 노출 | 별자리 배율이 R1부터 실제로 발동한다 |

### 10-3. 시작 별자리는 밸런스의 전제였다

피드백 장치로 도입했으나, 측정 결과 **없으면 어떤 곡선으로도 조건 2를 만족할 수 없다.**

| (sum 기준) | 시작 별자리 없음 | 있음 |
|---|---|---|
| greedy 부스판 클리어율 | 62.1% | **85.3%** |
| smart 풀버전 클리어율 | 18.8% | **28.1%** |
| R1 표준편차 | 0 | **28** |

---

## 11. 아트 파이프라인 (프로시저럴 생성 방식 확정)

### 11-1. 방식

**이미지 파일을 만들지 않는다.** 픽셀맵을 코드로 정의하고 런타임에 생성한다.

```
픽셀맵 정의 (칩 32×32 배열 · 카드 36×52 · 캐릭터 60×78)
   → 같은 크기의 오프스크린 Canvas에 1px 단위로 그림
   → toDataURL()로 1회 변환 후 useMemo 캐시
   → <img> 로 표시, CSS 폭 = 원본 × 정수배 + image-rendering:pixelated
```

원본을 **정수배로만** 확대하므로 **완벽한 도트 룩**이 나오고, rect를 수천 개 그리는 성능 문제도 없다.

> **규격의 원본은 11-4절이다.** 이 절은 파이프라인만 설명하며, 원본 크기·표시 배율이
> 충돌하면 11-4를 따른다.

### 11-2. 이 방식을 택한 결정적 이유 — 특수 조각 10종

특수 별자리 조각은 두 문양을 반반 합친 것이다. 손으로 그리면 **10장을 따로 그려야 한다.**
픽셀맵이 코드에 있으면 **합성 함수 하나로 10종이 자동 생성된다.**

**절단은 두 단계다.** 칩은 32×32이고 그 중앙 16×16이 문양이므로(11-4절), 절단선도 둘이다.

```ts
// ① 칩 전체 — 32열의 중앙, 즉 16열에서 자른다
function composeSpecial(a: SuitId, b: SuitId): PixelMap {
  const left  = CHIP[a].map(row => row.slice(0, 16));    // A 칩 좌측 16열
  const right = CHIP[b].map(row => row.slice(16, 32));   // B 칩 우측 16열
  return left.map((row, i) => [...row, ...right[i]]);
}

// ② 중앙 문양 — 문양 자체는 16×16이므로 8열에서 갈린다.
//    ①의 절단선이 문양 상자의 정중앙을 지나므로 자동으로 그렇게 된다.
```

배경·링·노치는 ①의 16열 절단으로, 중앙 문양은 그 결과로 ②의 8열 절단으로 갈린다.
링과 노치는 **세로 중심선에 대칭**이라 이음매가 보이지 않는다(11-4절).

기본 5종만 그리면 특수 10종은 공짜다. 색을 바꾸고 싶으면 상수 하나만 고치면 된다.

### 11-3. 파일 구조

| 파일 | 내용 |
|---|---|
| `src/assets/palette.ts` | 32색 팔레트 상수 |
| `src/assets/pixels.ts` | 문양 5종 + **별자리 아이콘 12종** + 떠돌이 + ORION + UI 아이콘의 픽셀맵 |
| `src/assets/compose.ts` | 특수 조각 합성 함수 |
| `src/ui/PixelSprite.tsx` | 픽셀맵 → Canvas → dataURL 렌더 컴포넌트 |

### 11-4. 규격

| 대상 | 원본 픽셀맵 | 표시 크기 |
|---|---|---|
| 칩 (기본·특수·떠돌이) | **32×32** | **64px** (2배) |
| 보드 셀 | — | **72px** (칩 64 + 여백 8) |
| **별자리 카드** | **36×52** | **72×104px** (2배) |
| 동반성 아이콘 | 16×16 | 32px |
| **ORION (M42)** | **60×78** | **120×156px** (2배) |
| **иєвυℓα** | **60×78** | **180×234px** (3배, 상점 전용 — 11-9절) |
| 문양 5종 (칩 내부) | 16×16 | 칩 중앙에 배치 |

> **칩이 16×16 → 32×32로 커진 이유**: 반지름 8px에 디테일 4겹을 넣으면 중앙에 3px만 남아
> 문양이 사라진다. 32×32의 **중앙 16×16이 정확히 기존 문양 크기**이므로,
> 칩을 키우면서 문양 5종은 한 장도 다시 그리지 않았다.

#### 칩 구조 (바깥에서 안으로)

| 겹 | 내용 | 색 |
|---|---|---|
| ① | 가장자리 노치 6개 (60° 간격) | **해당 문양의 밝은 변형색** |
| ② | 안쪽 링 1px | 바탕색의 어두운 변형 |
| ③ | 점선 원 1px | 밝은 색, 일정 간격 |
| ④ | 중앙 문양 | 기존 16×16 픽셀맵 그대로 |

노치를 크림색이 아니라 **문양별 밝은 색**으로 칠하면 카지노 칩 인상이 옅어지고,
별빛이 새어나오는 인상이 생기며, **특수 조각의 좌우 분할이 테두리에서도 드러난다.**

이 색 코딩은 좌우 분할의 **주 신호이지만, 10쌍 중 IMA&ACR 한 쌍에서는 성립하지 않는다.**
두 문양의 edge는 **ΔL\* 0.1**로 명도가 사실상 같고, **ΔE 19.8**은 5문양 전 조합 × 전 영역을
통틀어 최소값이다. 즉 이 한 쌍에서만 테두리가 분할을 알려주지 못하므로,
**여기서는 라벨 색 코딩이 보조 장치가 아니라 주 신호가 된다**(아래 ★절).

**측정** (`npx tsx tools/chip-contrast.mjs` · CIE76 · 원본 색 · 영역별 최소 쌍):

| 영역 | 최소 ΔE | 쌍 |
|---|---|---|
| field | 53.8 | IMA·MIM |
| edge | **19.8** (ΔL\* 0.1) | IMA·ACR |
| symbol | 29.2 | IMA·ACR |

CIE76의 JND는 2.3이다. 최소값 19.8조차 그 8배가 넘으므로 **구분 불가능한 쌍은 없다** →
**팔레트·픽셀맵은 변경하지 않는다.** 이 하한은 `tests/chip-contrast.test.ts`가 지킨다.

**특수 조각 합성**: 16열에서 자른다. 링·노치가 좌우 대칭이라 그대로 이어진다.
중앙 문양은 기존 규칙대로 좌 8열 + 우 8열.

#### ★ 특수 조각 라벨의 문양별 색 코딩 (P4-A)

특수 조각을 **라벨과 함께 표시하는 화면**에서는, 라벨 문자열의 각 문양 부분을
**해당 문양의 잉크 색**(`SUIT_INK`)으로 칠한다. 예: `IMA&ACR` → `IMA`는 Imai 파랑,
`ACR`는 Acrux 밝은 테두리색, `&`는 중립색.

**근거**: Acrux의 필드색(`#2B2B38`)이 배경(`#0A0A12`)에 거의 묻히므로, 그 절반에서
실제로 보이는 것은 칩 표면의 1/3을 차지하는 밝은 라벤더 테두리다. 그 결과 Acrux 절반의
평균색이 보라빛으로 읽혀 Mimosa 절반과 혼동된다.

> ⚠️ **이것은 보조 장치이지 대체물이 아니다.** 칩 자체의 판독성을 라벨이 대신할 수는
> 없다 — 보드 위와 덱 구성 패널에는 라벨이 없다. 칩 판독성 자체의 처방은
> `tools/chip-contrast.mjs`의 측정 결과를 보고 따로 판단한다.

구현: `src/ui/Shop.tsx`의 `SpecialEntry`.

### 11-5. 별자리 12종 — 세로 직사각 카드

**칩이 아니다.** 36×52 픽셀맵의 **트럼프 카드 형태**이며, 표시 크기는 72×104px다.

> 32×48 → 36×52로 키운 이유는 **프레임에 2px를 확보하면서 성도 영역(26×40)을 그대로 유지**하기 위함이다.
> 성도가 줄면 별자리 형상이 무너진다.

> ⚠️ **"판정 패턴 도식" 안은 폐기됐다.** 조건의 기하 도식(점 N개를 축 방향으로 배열)은
> 판독은 되지만 우주 테마를 잃는다. 카드는 **실제 성도(星圖) 이미지**를 싣는다.

#### 카드 구성

| 요소 | 내용 |
|---|---|
| 프레임 | **2px** — 테두리 선 + 성좌 장식 + 둥근 모서리 |
| **배경** | **별하늘 픽셀 아트** (아래 참조) |
| 중앙 | 해당 별자리의 **실제 성도** |
| 별 | 십자 발광 (밝은 코어 + 4방향 팔), 등급별 크기·밝기 차등 |
| 연결선 | 별보다 어두운 청색 계열 |

#### 배경 — 카드마다 다른 별하늘

| 요소 | 내용 |
|---|---|
| 바탕 | 어두운 남보라 |
| 성점 | 불규칙 산포 — **1px 점 · 2px 십자 · 드물게 밝은 별** |
| 성운 얼룩 | 1~2군데, **카드 고유 색의 아주 어두운 변형** |

**12종이 각자 다른 하늘을 갖는다.** 배치는 별자리 인덱스로 시드된 난수에서 나오므로
카드를 다시 그려도 같은 하늘이 나온다(CLAUDE.md §8). 열두 장을 늘어놨을 때
한 배경의 복사본 열두 개로 보이면 안 된다.

> 🚫 **5×5 판정 격자 좌표계 안은 폐기됐다.** 성도 뒤에 게임 보드를 겹쳐 판정 셀을
> 강조하는 방식은 판독에는 도움이 되지만, 카드가 **성도가 아니라 도표**가 된다.
> 판독 문제는 아래처럼 텍스트로 푼다.

#### 프레임 장식 — 2톤 성좌

카드 고유 색에서 두 톤을 만든다.

| 톤 | 만드는 법 | 쓰는 곳 |
|---|---|---|
| 진한 톤 | 고유 색을 배경 쪽으로 약간 눌러서 | 테두리 선, 별을 잇는 연결선 |
| 연한 톤 | 진한 톤에 흰색을 섞어서 | 별의 코어, 모서리 앵커 |

테두리 둘레 전체에 별을 띄엄띄엄 놓고 연결선으로 잇는다.
**네 모서리의 별은 더 크고 밝게** 두어 앵커 역할을 시킨다.
12종이 각자 고유 색을 쓰되 **장식 패턴은 완전히 동일**하다 — 카드가 한 세트로 보여야 한다.

#### ★ 밝기 위계 (절대 규칙)

> **프레임 별의 최대 밝기 < 성도 별의 평균 밝기.**
> 프레임이 성도보다 밝으면 시선이 테두리에 뺏겨 카드를 읽을 수 없다.

이 조건은 **눈대중이 아니라 계산으로 강제**한다. 연한 톤은 카드의 성도 평균 밝기의
80% 아래로 내려갈 때까지 배경 쪽으로 단계적으로 눌린다. 테스트가 12종 전부를 검사한다.

> ⚠️ 그래서 **진한 톤은 고유 색 원본 그대로가 아니다.** 청록·호박처럼 원래 밝은 색조는
> 원본 그대로 두면 안쪽 성도와 밝기가 맞먹어 위계가 깨진다.
> 위 ★ 조건과 "원본 그대로" 지시가 충돌하므로 **★를 우선**했다.
> 색조는 유지되므로 축 식별에는 영향이 없다.

성도 자체는 **전 카드 청백색으로 통일**한다. 별자리 형상은 어느 축이 발동하는지를
전혀 말해주지 않으므로, 축 구분은 **프레임 색**이 전담한다.

#### 축 색상 코딩 — 프레임 색으로 유지

| 계열 | 별자리 | 프레임 색 |
|---|---|---|
| 세로 | 양자리 · 염소자리 · 궁수자리 | **청록** |
| 가로 | 천칭자리 · 물고기자리 · 사자자리 | **호박** |
| 대각 | 쌍둥이자리 · 황소자리 · 전갈자리 | **자홍** |
| 도형 | 물병자리 · 처녀자리 | **연보라** |
| 전역 | 게자리 | **수소적색** |

> 이 색 코딩은 밸런스와도 직결된다. 게임 시작 시 플레이어는
> **양자리(세로) / 천칭자리(가로)** 중 하나를 고르는데(10-3절),
> 축이 색으로 즉시 구분되지 않으면 그 선택이 무의미해진다.

#### ★ 판독성은 텍스트 병기로 해결한다 (절대 규칙)

성도는 "이게 무슨 별자리인가"만 말할 뿐 **연속 몇 개인지, 배율이 얼마인지는 말하지 못한다.**

> **양자리(세로 3연속)와 천칭자리(가로 3연속)는 그림만으로 절대 구분할 수 없다.**
> 두 규칙은 축만 다른데, 두 성도는 서로 닮은 구석이 전혀 없다.
> 격자 좌표계를 폐기한 이상 이 격차를 그림으로 메울 방법은 없다.

따라서 **카드를 단독으로 표시하지 않는다.**

```
[카드 이미지]
 궁수자리
 세로 5연속
 ×2.5
```

| 규칙 | 내용 |
|---|---|
| 표시 단위 | 카드 이미지 + **이름 · 조건 · 배율**이 항상 한 덩어리 |
| 구현 강제 | `ui/ConstellationCard.tsx` 하나만이 카드를 화면에 올린다. 이 컴포넌트는 텍스트를 뗄 수 없다 |
| 금지 | `PixelSprite`에 카드 픽셀맵을 직접 넘겨 단독 표시하는 것 |
| 문자열 출처 | `CONSTELLATION_RULES` · `CONSTELLATION_MULTIPLIERS`에서 파생 |

카드와 엔진이 서로 다른 규칙을 말하는 일이 없어야 한다.

> 성도 좌표는 **양식화된 성도**다. 이 크기에서 각 별자리의 알아볼 수 있는 몸짓을 살린 것이지
> 실측 천문 좌표가 아니다.

### 11-6. 떠돌이 칩 외형

**다른 조각과 완전히 동일한 칩 레이아웃**을 쓴다 — 노치 6개, 안쪽 링, 점선 원, 중앙 문양.
보드 위에서 이것은 별종이 아니라 **조각 하나**여야 하므로, 실루엣으로 구별하지 않는다.

| 요소 | 내용 |
|---|---|
| 레이아웃 | 기본 조각과 동일 (11-4절 4겹 구조) |
| 바탕 | **무지개 그라데이션** — 정해지지 않은 상태의 시각화 |
| 중앙 문양 | **왕관** (16×16, `pixels.ts`) |
| 링·문양 색 | 어두운 색 — 무지개 어느 구간에서도 대비가 확보된다 |

- 무지개는 **Ginan(적) → Gacrux(녹) → Imai(청) → Mimosa(자)** 를 잇는 연속 그라데이션이다.
  **Acrux(검정)는 제외한다** — 빛의 색이 아니고, 밴드로 넣으면 칩을 가로지르는 죽은 띠가 된다.
- 테두리·노치·점선 원도 무지개를 따라간다. 흰 테두리로 두면 노치와 점선이 칩 표면의 1/3이라
  무지개를 통째로 삼킨다.
- 6번째 기본 문양처럼 보이면 안 된다 → **왕관**이 그 역할을 한다.
  다섯 문양 중 어느 것도 아니면서 "무엇이든 될 수 있다"를 말한다.

> 초기안(육각 결정 실루엣)은 폐기했다. 실루엣을 다르게 하면 혼동은 사라지지만
> 보드 위에서 혼자 튀어 **조각이 아니라 이물질로 읽힌다.**

### 11-7. 팔레트 배분 (32색)

| 용도 | 색 수 |
|---|---|
| 칩 5종 × (기본·하이라이트·섀도) | 15 |
| **동반성 티어 프레임 5종 (예약)** | 5 |
| 배경 · 성운 계열 | 6 |
| UI 중립색 | 6 |

> ⚠️ **티어 프레임 5색을 반드시 예약**해 둘 것. P4에서 동반성 30종에
> Rare/SR/Epic/Mythic/Legendary 프레임이 필요한데, 32색을 지금 다 쓰면 자리가 없다.

> ⚠️ **Acrux(`#2B2B38`) 주의**: 어두운 배경 위에서 묻힌다.
> 섀도 변형은 사실상 무의미하므로, 하이라이트와 테두리에 대비를 몰아줄 것.

#### ★ 32색 예산이 세는 것 — 원색이지 파생 톤이 아니다 (확정)

11-5절의 카드 2톤과 11-9절의 `NEBULA_INK`·`NEBULA_GLOW`는 팔레트 밖의 색이 아니다.
이 절의 예산과의 관계를 확정한다.

| 구분 | 예산에 포함되는가 |
|---|---|
| **원색 상수** — `palette.ts`의 `PALETTE` 객체에 손으로 적은 색 리터럴 | **포함.** 이 개수가 32다 |
| **파생 톤** — 원색에서 `mix()`로 계산되는 색 | **포함하지 않는다** |

> **새 색 리터럴을 `PALETTE` 밖에 손으로 적는 것은 금지한다.** 파생 톤이 예산에서
> 빠지는 근거가 "원색에서 결정론적으로 계산된다"는 것뿐이기 때문이다. 손으로 적는 순간
> 그것은 33번째 색이며, 팔레트를 한 곳에서 바꾼다는 규칙도 함께 깨진다.
>
> 현재 구현이 그렇다: `NEBULA_INK`·`NEBULA_GLOW`·`TIER_COLOURS`·`AXIS_COLOURS`·
> `SUIT_INK`와 카드의 `frameTones()`는 전부 `PALETTE` 상수 + `mix()`로 계산된다.
> `tests/assets.test.ts`가 `PALETTE` 값의 개수가 32 이하이고 중복이 없음을 검사한다.

#### 성점 · 연결선 · 카드 프레임 색의 출처

**새 색을 추가하지 않았다.** 티어 프레임 5색은 손대지 않고, 아래처럼 기존 배분 안에서 겸용한다.

| 카드 요소 | 쓰는 색 | 원래 용도 |
|---|---|---|
| 프레임 | 성운 5색 | 축 색상 코딩 (11-5절) |
| 카드 배경 | 성운 심연색 | 배경 |
| 별 코어 | `starWhite` | 밝은 텍스트 · 떠돌이 테두리 |
| 별의 팔 | `starGlow` | 흐린 텍스트 |
| 연결선 | `starLink` | 패널 헤어라인 |
| 성점 | `panelEdge` | 패널 테두리 |

성운 계열이 배경과 축 코딩을 겸하는 것은 의도다. 축 구분에 필요한 5개의 뚜렷한 색조는
실제 성운의 색 분포와 정확히 같은 범위이므로, 코딩이 **범례가 아니라 천문**으로 읽힌다.

### 11-8. ORION 디자인 지침

**역할**: 플레이 중 해설·반응을 담당하는 동반자다. 상점 딜러는 иєвυℓα(11-9절)가 맡는다.
디자인 지침 자체는 아래 그대로 유지한다.

- 인간형이되 **머리와 팔만** 사람과 유사한 신체 구조
- 몸통 아래는 오리온 대성운의 형상 — 붉은 수소 발광(Hα) + 푸른 반사성운의 그라데이션
- 귀여운 도트 캐릭터 톤 (위협적이지 않게)
- 표정 4종: 기본 / 놀람 / 만족 / 거래
- **60×78 픽셀맵**은 유일하게 손이 많이 가는 부분 → **P4 단계에서 별도 집중 작업**

> 초기 규격 48×64(96×128 표시)에서 **60×78(120×156 표시)**로 키웠다.
> 참조 영상의 캐릭터가 화면 높이의 약 24%를 차지하는데, 96×128은 630px 캔버스에서 20%에 그쳐
> 해설자가 화면 구석의 장식으로 읽혔다. 156px는 캔버스 높이의 24.8%다 (11-10절).

> 표정 4종 중 '거래'는 딜러였던 시절의 잔재다. 해설자에게 무엇이 필요한지는
> 대사 시스템이 붙은 뒤 P4에서 재검토한다.

### 11-9. иєвυℓα 설정 · 디자인 지침 (P4-A 확정)

**P4-A에서 스프라이트와 대사를 구현했다.** 아래는 그 확정 내용이다.

| 항목 | 내용 |
|---|---|
| 표기 | **`иєвυℓα`** — 이 표기를 그대로 UI에 노출한다. 한글로 옮기지 않는다 |
| 정체 | 특정 성운이 아니다. **실존 천체가 아닌 신비로운 존재** |
| 메인 컬러 | 보라 · 자주 계열 |
| 역할 | 상점 딜러 — 별자리 조각 · 특수 조각 · 별자리 · 동반성 거래 |
| 톤 | 능글맞은 상인 |

**ORION과의 대비**가 이 캐릭터의 존재 이유다.

| | ORION (M42) | иєвυℓα |
|---|---|---|
| 실재성 | 실존 천체 (오리온 대성운) | 실존하지 않음 · 정체불명 |
| 성격 | 차분한 관측자 | 능글맞은 상인 |
| 등장 | 플레이 중 (해설) | 라운드 사이 (상점) |
| 컬러 | 붉은 Hα + 푸른 반사성운 | 보라 · 자주 |

#### ★ 확정 디자인 (P4-A · 2차 개정)

**초안은 폐기했다.** 큰 후드 타원 + 검은 얼굴 구멍 + 매끈한 원뿔 로브는 **로켓이나 촛불로
읽혔고 생명체로 보이지 않았다.**

**1차 개정은 금지 조건을 넘었을 뿐 목표에 도달하지 못했다.** 후드 안이 구멍이 아니고
밑단이 대칭이 아니게 됐지만, 실루엣 전체를 같은 밝기의 분홍 테두리가 둘러서 **배경에 붙인
보라색 인형**으로 읽혔다. 2차 개정은 그 넷을 고친 것이며, 아래가 확정안이다.

| 항목 | 확정 |
|---|---|
| 픽셀맵 | **60×78**. 상점 표시 **3배 = 180×234** (플레이 화면의 ORION은 2배 120×156 유지) |
| 생성 방식 | 손으로 그리지 않는다. **기하학으로 계산**한다 (11-1절) |
| 골격 | **인간형을 분명히 한다.** 후드 머리(높이의 약 1/4) → **목** → **어깨** → **소매 2개** → 허리 → 로브 → 밑단이 흩어져 소멸 |
| 얼굴 | **후드 안은 어둡되 배경색이 아니다.** 그 안에 **빛나는 눈 한 쌍**이 있고, 눈에서 바깥으로 빛이 단계적으로 약해진다 |
| **테두리** | **균일한 밝기 금지.** 후드 광원(눈)으로부터의 **거리 함수로 감쇠**시킨다. 최대 밝기는 **눈 코어 밝기 아래로 계산이 강제**한다 — 11-5절의 카드 프레임과 같은 방식으로, 한계에 들 때까지 밝은 톤을 배경색 쪽으로 단계적으로 눌러 만든다. 가장 먼 끝은 베일 색 그 자체이므로 그 부근에는 **테두리가 아예 없다** |
| **목** | 후드와 어깨 사이에 **1~2px**. 머리가 어깨에 바로 얹히면 둘이 한 덩어리로 읽힌다 |
| **팔** | 소매는 로브와 **luma 차 10 이상**(현재 12.6). 그리고 **소매 끝으로 갈수록 좁아지는 실루엣**이어야 한다 — 위아래 굵기가 같은 둥근 형태는 벙어리장갑으로 읽힌다. 안쪽 모서리는 몸통에 붙여 계산하고(팔이 몸에서 떨어지지 않도록), 바깥 모서리만 좁힌다 |
| **밑단** | **선으로 끝나지 않는다.** 윤곽선(=한 열당 바닥 한 줄)을 계산해 자르지 않고, **행마다 남길 픽셀 수를 정해** 아래로 갈수록 줄인다. **중앙 홈 금지.** 좌우 대칭 금지 — 남는 쪽의 중심선을 `y`에 따라 좌우로 흘려 기울인다. 난수는 여전히 쓰지 않는다 |
| 반응 | 표정이 아니라 **빛의 세기**. `idle` / `keen` / `dealt` 3단, 뒤로 갈수록 밝다. 테두리도 같은 광원에서 파생되므로 함께 밝아진다 |

> **바닥에 서지 않고 아래로 흩어진다.** 이것이 "망토 입은 사람"과 "미지의 존재"를 가르는 지점이다.
> ORION은 성운을 몸으로 가진 사람이고, иєвυℓα는 사람의 형태를 흉내 낸 성운이다.
> — 다만 **먼저 사람으로 읽혀야** 그 대비가 성립한다. 초안이 실패한 지점이 정확히 여기다.

> **밑단에서 "밀도로 사라진다"와 "테두리가 사라진다"는 한 쌍이다.**
> 선명하게 닫힌 경계선을 두른 채로는 흩어질 수 없다. 테두리 감쇠(위)와 밀도 감소(위)는
> 같은 목적의 두 절반이며, 하나만 지키면 둘 다 실패한다.

#### 남은 개정 (P4-A에서 중단, 수동 작업 예정)

**2차 개정은 여기서 멈췄다.** 위 확정 표는 도달한 지점이 아니라 **지향점**이며,
아래 넷은 아직 그 표를 만족하지 못한다. 남은 작업은 **수동으로 진행한다** — 코드 자동
생성으로 반복 시도하는 것이 여기서부터 비효율이라고 판단했다.

| # | 항목 | 현재 상태와 요구 |
|---|---|---|
| 1 | **실루엣 방향** | 지금은 어깨에서 최대폭을 찍고 밑단까지 **단조 감소**해 한 점으로 끝난다 → 쐐기·불꽃·**체스 말**로 읽힌다. 허리 아래부터 밑단까지는 폭이 **증가**해야 옷을 입은 존재가 된다. 방향 자체가 반대이므로 상수 조정이 아니라 재설계다 |
| 2 | **팔** | 어깨에서 뻗은 삼각 날개는 팔이 아니라 **망토 깃**으로 읽힌다. 팔을 읽히게 하는 **주 신호는 톤 차가 아니라 몸통과 팔 사이 1~2px 틈**이다. 톤은 보조 수단일 뿐 — luma 차 12.6은 확보돼 있는데도 팔로 읽히지 않는 것이 그 증거다 |
| 3 | **밑단** | 중앙 홈은 제거됐으나 여전히 **통짜 도형의 꼭짓점**으로 끝난다. 11-9가 요구하는 "가닥으로 흩어짐"은 **3~5가닥 + 결정론적 디더 소멸**이다. 난수는 여전히 금지(되돌리면 안 되는 것 #1) |
| 4 | **눈·후드 내부** | 눈이 큰 원형이라 **만화 눈**이고, 눈 **아래**가 가장 어두운 별개 덩어리로 뭉쳐 **수염**처럼 읽힌다. 확정 표의 "눈에서 바깥으로 빛이 단계적으로 약해진다"와 **감쇠 방향이 반대**다 |

##### ★ 이미 달성됐다 — 되돌리지 말 것

수동 작업 중에도 아래는 유지한다. 전부 실패를 거쳐 얻은 것이고, 되돌리면
"되돌리면 안 되는 네 가지"를 다시 어기게 된다.

| 달성 항목 | 내용 |
|---|---|
| **테두리 광원 감쇠** | 균일한 밝은 외곽선을 제거했다. 후드 광원으로부터의 거리로 감쇠하며 최대 밝기는 눈 코어 아래로 계산이 강제한다 (금지 #4) |
| **목 1~2px** | 후드와 어깨 사이에 목이 있다. 머리와 몸이 분리돼 보인다 |
| **후드 안이 `void`가 아님** | 관통상으로 읽히지 않는다 (금지 #3) |
| **밑단 좌우 비대칭** | 로켓 노즐로 읽히지 않는다 (금지 #2) |
| **밝기 위계 luma 65 / 78 / 87** | 베일 / 소매 / 주름. 소매-로브 차 12.6으로 10 이상 규칙 충족 |

> **항목 2와 "이미 달성됨"의 luma 65/78/87은 모순이 아니다.**
> 톤 차는 규칙대로 확보됐고 유지해야 한다. 다만 그것만으로는 팔이 읽히지 않는다는 것이
> 2차 개정이 알아낸 사실이며, 그래서 항목 2의 주 신호가 **틈**으로 바뀐 것이다.

`tests/assets.test.ts`의 검사 5종은 **그대로 둔다.** 수동 작업 중 위 "이미 달성됨"이
되돌아가는 것을 막는 가드 역할을 한다.

#### ★ 되돌리면 안 되는 네 가지 (실패로 확인됨)

| # | 규칙 | 어겼을 때 |
|---|---|---|
| 1 | **픽셀 단위 난수 금지** | 1px 가시가 서서 밑단이 잡티가 된다. 저주파 사인파만 쓴다 |
| 2 | **어깨 아래 좌우 대칭 금지** | 밑단이 완전 대칭 원뿔이 되어 **로켓 노즐**로 읽힌다 |
| 3 | **후드 안을 `void`로 칠하지 말 것** | 얼굴 모양으로 뚫린 구멍이 되어 **관통상**으로 읽힌다 |
| 4 | **균일한 밝은 외곽선 금지** | **배경에 붙인 스티커**로 읽히고, 테두리가 눈보다 밝아 **시선이 얼굴에 가지 않는다** |

`tests/assets.test.ts`가 넷을 다섯 개의 검사로 확인한다.

| 검사 | 규칙 |
|---|---|
| 어깨 아래 좌우 대칭 일치율 90% 미만 | 2 |
| `void` 픽셀 0개 · 눈 2개 분리 | 3 |
| 외곽 픽셀 최대 밝기 < 눈 코어 밝기 (3개 무드 전부) | 4 |
| 외곽 픽셀 최소 밝기 = 베일 색 (감쇠이지 단일 톤이 아님) | 4 |
| 허리 아래 각 행의 채움 픽셀 수가 윗행 이하 · 마지막 행 0 | 1, 2 |

> 형태의 좋고 나쁨은 테스트가 판정하지 않는다. 테스트는 **되돌아가는 것만** 막는다.

#### 팔레트 — 파생 톤으로 해결 (확정)

32색(11-7절)이 이미 전부 배정돼 전용 색이 없으므로, 별자리 카드와 같은 방식으로
**기존 색의 파생 톤**을 쓴다. `src/assets/palette.ts`의 `NEBULA_INK` · `NEBULA_GLOW`.

| 후보 | 판정 |
|---|---|
| `mimosa` 3색 | ❌ MIM 칩 전용. 상점 주인을 보드 위 조각과 같은 보라로 입힐 수 없다 |
| `nebulaMagenta` | ❌ 대각 축 코딩 전용(11-5절). 캐릭터에 쓰면 축 색의 의미가 흐려진다 |
| `tierEpic` | ❌ 동반성 티어 예약 |

→ 베일은 **마젠타를 `nebulaDeep` 쪽으로 깊이 눌러** 만든다.
**mimosa와는 색상뿐 아니라 명도로 벌린다** — 테스트가 luma 차이 40 이상을 강제한다.
**동반성 티어 프레임 5색은 건드리지 않았다.**

**밝기 위계 (확정, `idle` 기준 luma).** 후드에서 나오는 빛만 밝을 수 있다는 규칙의 실제 값이다.

| 요소 | luma | 근거 |
|---|---|---|
| 베일 (로브·후드) | 65 | 바탕 |
| 소매 | 78 | 로브와의 차 **12.6** — 10 이상 규칙(위) 충족 |
| 옷 주름 | 87 | 허리 위에서만 그린다. 아래는 가스이므로 주름이 없다 |
| 테두리 (최대, 후드 부근) | 131 | 눈 코어의 0.8배 이하로 **계산이 강제** |
| 테두리 (최소, 밑단) | 65 | 베일과 같음 = 테두리 없음 |
| 눈 코어 | 175 | 화면에서 가장 밝다 |

`keen` · `dealt`에서는 광원이 밝아지므로 테두리 최대와 눈 코어가 함께 올라가고,
0.8배 관계는 계산으로 유지된다 (`nebulaRim`, `src/assets/compose.ts`).

### 11-10. ★ 화면 레이아웃 — 고정 캔버스 (절대 규칙)

**게임 화면은 1120×630 고정 캔버스다. 유동 웹 레이아웃으로 되돌리지 않는다.**

#### 왜 고정인가

유동 레이아웃에서는 크기가 픽셀로 박힌 요소(보드)만 제자리를 지키고 나머지는 flex 여백을 따라
흩어진다. 2560px 모니터에서 보드는 360px 그대로인데 손패·정산·ORION이 사방으로 밀려나
**참여자마다 다른 화면을 보게 된다.** 요소를 개별 이동해서는 해결되지 않는다 — 레이아웃이
유동이기를 그만두어야 한다. 부스는 서로 다른 노트북 3대 이상으로 운영되므로(12-2절)
**모든 기기에서 같은 그림**이 나오는 것이 요구사항이다.

#### 방식

| 항목 | 값 |
|---|---|
| 논리 캔버스 | **1120×630** (16:9) |
| 배치 | 전 요소 **절대 좌표** |
| 창 맞춤 | 컨테이너에 `transform: scale()` |
| 배율 | `min(창폭/1120, 창높이/630)`을 **정수로 내림** |
| 남는 영역 | `void` 색 레터박스 |
| 스프라이트 | `image-rendering: pixelated` 유지 |

> ⚠️ **배율을 정수로 내리는 이유**: CLAUDE.md §7과 11-4절이 **정수배 스케일만** 허용한다.
> 1366×768의 원시 배율은 1.22, 2560×1440은 2.28이다. 소수배로 그리면 32×32 칩이
> 픽셀 경계에 걸치지 않아 도트 격자가 무너진다. 내림하면 각각 **1배·2배**가 되며,
> 이는 레이아웃이 애초에 설계된 배율이다.
>
> 캔버스보다 **작은 창**에서만 예외로 소수배를 쓴다. 내림하면 0이 되기 때문이며,
> 잘려 보이는 것보다 작게라도 전부 보이는 편이 낫다.

#### 배치 좌표 (1120×630 기준)

| 요소 | X | Y | 크기 |
|---|---|---|---|
| 스타더스트 | 16 | 12 | — |
| 라운드/턴 | 중앙 560 | 16 | — |
| 상태 텍스트 | 중앙 560 | 48 | — |
| DEV 버튼 | 1050 | 12 | 우측 정렬로 열림 |
| **보드** | 40 | 106 | **368×368** (셀 72 + 간격 2) |
| 보유 별자리 라벨 | 432 | 70 | — |
| **별자리 카드 2×2** | 432 | 94 | 셀 72, 간격 8 (엔트리 142) |
| 턴 종료 버튼 | 432 | 520 | 88×44 |
| **정산 패널** | 584 | 90 | **516×150** (문양 5컬럼 × 100) |
| 합산식 | 우측정렬 1096 | 258 | — |
| 큰 누적 점수 | 중앙 842 | 330 | 목표·진행바 포함 |
| 말풍선 | 700 | 470 | 250×90 |
| **ORION** | 980 | 474 | **120×156** |
| 손패 아치 | 20 | 496 | 400×104 |
| 손패 카운터 | 20 | 606 | — |

#### ★ 별자리 카드는 2×2다 — 세로 열은 불가능하다

11-5절은 **카드에서 이름·조건·배율 텍스트를 뗄 수 없다**고 못박는다. 이 텍스트를 포함하면

```
카드 이미지 104 + 텍스트 3줄 = 엔트리 142 (실측)
세로 4장: 142×4 + 간격 8×3 = 592  →  y=94 시작 시 686 (캔버스 630 초과)
게다가 턴 종료 버튼(y=520)을 관통한다
```

**2×2는 292px에 들어간다** (실측 y 94–386, 버튼까지 134px 여유).
카드 이미지만 계산하면 세로 열도 434px로 들어가지만,
그 계산은 11-5절이 금지한 "텍스트 없는 카드"를 전제한 것이다.

라벨을 y=70에 두는 이유도 같은 종류다. 카드 열은 x=432에서 시작하고 상태 텍스트는 560을
중심으로 놓이므로, 라벨을 격자 위(y=52)에 두면 상태 텍스트와 부딪힌다.

이 때문에 정산 패널이 560 → **584**로 24px 밀렸고, 폭은 540 → **516**이 됐다.
문양 5컬럼에 100px씩 배분된다.

#### 배치 좌표 — 상점 (1120×630 기준, P4-A)

상점도 **같은 평면·같은 규칙**이다. 좌표 정본은 `src/ui/Canvas.tsx`의 `SHOP_LAYOUT`이고
아래는 그 사본이다(위 게임 화면 표와 같은 관계).

**진열은 왼쪽 열**, **덱 정보는 오른쪽 위**, **иєвυℓα는 우하단**이다.
그녀는 이 화면의 유일한 캐릭터이고 자기 가게에 있으므로 **3배로 키워 바닥까지 세운다.**
(초안은 2배로 위쪽 구석에 뒀는데 화면 하단 1/3이 통째로 비었다.)

| 요소 | X | Y | 크기 |
|---|---|---|---|
| 스타더스트 | 16 | 12 | — |
| 상점 제목 | 중앙 560 | 14 | — |
| 다음 라운드·목표 안내 | 중앙 560 | 44 | — |
| 특수 조각 라벨 | 24 | 82 | — |
| **특수 조각 4** | 24 | 100 | **600×110** (엔트리 142, 간격 10) |
| 별자리 라벨 | 24 | 232 | — |
| **별자리 2** | 24 | 250 | **600×120** (엔트리 294, 간격 12) |
| 동반성 라벨 | 24 | 392 | — |
| **동반성 2 (잠김)** | 24 | 410 | **600×110** (엔트리 294, 간격 12) |
| 리롤 버튼 | 24 | 544 | 200×48 |
| 다음 라운드 버튼 | 248 | 544 | 200×48 |
| 리롤 안내 텍스트 | 24 | 602 | — (두 버튼 아래) |
| **덱 구성 패널** | 664 | 72 | **440×214** (문양 5행 × 32) |
| **BLACK-HOLE** | 664 | 296 | 440×86 |
| 말풍선 | 664 | 424 | 256×104 |
| **иєвυℓα** | 932 | 396 | **180×234** (60×78의 3배) |
| 별자리 교체 프롬프트 | 중앙 560 | 150 | 648×268 — **엔트리** 96 × 5장 + 간격 12 + 화살표 32 |

> **덱 패널의 행 높이가 32인 이유**: 11-4절의 칩 원본이 32×32이고 1배로 그리므로
> 그보다 낮은 행에는 들어가지 않는다.
>
> **덱 패널이 BLACK-HOLE 바로 위에 있는 이유**: 8-1절이 STAR-CHART를 "BLACK-HOLE 옆
> 상시 표시"로 못박는다. P5에서 STAR-CHART가 들어올 자리가 이 패널이므로, 지금부터
> 이웃해 있어야 한다.
>
> **교체 프롬프트에 좌표가 있는 이유**: 별자리 4개 보유 상태에서만 열리므로 스크린샷으로
> 확인하기 가장 어려운 화면이다. 카드 5장이 캔버스 안에 들어오는지를 테스트가 대신 본다.
>
> ⚠️ **엔트리 96은 카드 이미지 폭이 아니다.** 카드 이미지는 36×52 픽셀맵의 **2배 = 72×104**
> (11-4절, CLAUDE.md §7은 정수배만 허용한다). 96은 그 이미지에 텍스트 열 폭과 좌우 4px
> 여백을 더한 **엔트리 하나의 자리**다. 96을 이미지 폭으로 읽으면 2.67배가 되어 §7 위반이
> 되므로, `SHOP_LAYOUT.replace`는 `card`(엔트리)와 `cardImage`(이미지)를 따로 둔다.

#### 검증 조건

- `tests/canvas.test.ts`가 배율표와 좌표 상자 겹침을 검사한다 (게임 화면·상점 각각)
- 1366×768에서 **1배**, 2560×1440에서 **2배**로 뜰 것
- 어떤 상자도 캔버스 밖으로 나가지 않을 것
- **상점 상자는 서로 겹치지 않을 것** — 전 쌍 검사
- ★ **모달 오버레이는 전 쌍 검사에서 제외한다.** 모달은 자기가 덮는 화면 위에 뜨는 것이
  존재 이유이므로 겹침이 정상이다(별자리 교체 프롬프트 ↔ 덱 구성 패널). 대신
  **① 캔버스 안에 들어올 것 ② 내용물이 모달 상자 안에 들어올 것** 두 가지를 따로 검사한다
- ★ **`SHOP_LAYOUT`의 키 집합과 위 상점 좌표표의 행이 일대일 대응할 것.**
  표는 사본이므로 대조하지 않으면 어긋난다 — 테스트가 이 대응을 강제한다

---

## 12. 부스 운영 제약 ⚠️ (1인 운영 확정)

개발자 겸 운영자가 **1명**이다. 이는 게임 설계에 직접 영향을 준다.

### 12-1. 처리량 계산

| 조건 | 시간당 참여자 |
|---|---|
| 풀버전 40분 × 노트북 1대 | **1.5명** |
| 풀버전 40분 × 노트북 3대 | 4.5명 |
| 부스판 15분 × 노트북 3대 | **12명** |

> ⚠️ **이 표는 플레이 시간만 계산한 값이다.** 룰 설명·튜토리얼·좌석 교대·리셋에 드는
> 회전 시간은 포함하지 않았으므로, 실제 처리량은 이보다 낮다.
> 13-2절이 말하는 **20분은 총 체류 시간**(설명 + 플레이 + 교대)이며 같은 값의 다른 표기가
> 아니다 — 부스판 플레이 15분에 회전 5분을 더한 쪽에 가깝다.
>
> **실제 처리량 재산정은 P6 부스 운영에서 다룬다.** 여기 숫자는 지금 고치지 않는다.

GBL은 **참여 중학생의 투표**로 순위가 정해진다.
표를 던질 사람 수가 곧 점수이므로, **처리량이 곧 성적**이다.

### 12-2. 그래서 필요한 것

1. **완전 자립형 튜토리얼** — 운영자가 옆에 붙어 설명하지 않아도 시작할 수 있어야 한다.
   1인 운영에서는 이게 선택이 아니라 필수다.
2. **노트북 3대 이상 동시 운영** — 브라우저 실행이므로 대수 확장은 자유롭다.
3. **부스판(3라운드 ≈ 15분) 분기** — Phase 7에서 최종 판단.
   풀버전 코드에서 `TOTAL_ROUNDS`와 `TARGET_SCORES`만 교체하면 되도록 설계한다.
4. **원클릭 리셋** — 다음 참여자를 위해 즉시 초기화.
5. **로컬 리더보드** — 운영자 개입 없이 경쟁 심리 유발.

### 12-3. 설계 반영

`src/core/config.ts`에 부스 모드 프리셋을 함께 정의한다.

```ts
export const MODE_PRESETS = {
  full:  { TOTAL_ROUNDS: 8, TARGET_SCORES: [490, 630, 640, 880, 1080, 1250, 1330, 1530] },
  booth: { TOTAL_ROUNDS: 3, TARGET_SCORES: [490, 630, 640] },
};
```

> 목표치는 10절이 원본이고 P2-B에서 역산 확정됐다. 이 블록은 **구조**를 보이기 위한 것이므로,
> 수치를 여기서 고치지 말고 10절과 `config.ts`를 따라간다.

### 12-4. 부스 모드의 밸런스 철학은 풀버전과 정반대다

| | 풀버전 | 부스판 |
|---|---|---|
| 목표 클리어율 | **20 ~ 30%** (도전) | **70% 이상** (만족) |
| 최적화 대상 | 재도전 욕구 | **투표** |

부스 참여자는 단 한 번 플레이하고 투표한다. 실패하고 떠나면 표를 잃는다.

> **판정 기준은 개별 숫자가 아니라 클리어율이다.**
> 초기 문구는 "상향 금지"였으나, 이는 개별 목표값이 아니라 **난이도**를 뜻한다.
> 실제 확정안에서 R2는 500 → 630으로 올랐지만, 기존 300·500은 바닥값 600보다 낮아
> 아무도 탈락시키지 못하는 장식이었고 실질 관문은 R3(800) 하나뿐이었다.
> R3를 640으로 크게 낮춘 결과 greedy 클리어율이 **28.4% → 72.3%** 로 개선됐으므로
> 규칙의 취지는 지켜졌다.
>
> (이 두 수치는 떠돌이 가격 10 시점의 측정이다. 무상 지급까지 반영한 최종값은 **85.3%** 다 — 13-6절.)

---

### 12-5. ★ 부스판 WAGER 보정 원칙 (신규)

풀버전 측정에서 WAGER 정답률 0% ↔ 100%의 클리어율 차이가 **27.0%p**로 나타났다.
학습 행동이 승패에 유의미해진 것은 성과지만, **부스판에 그대로 적용하면 재앙이다.**

> 부스 참여자는 **확률을 배우러 온 중학생**이다.
> 확률 문제를 못 맞혀서 탈락한다면, 게임이 가르치려는 바로 그 대상을 걸러내는 것이다.

따라서 두 버전에 **서로 다른 원칙**을 적용한다.

| | 풀버전 | 부스판 |
|---|---|---|
| WAGER의 역할 | 승패를 가르는 실력 요소 | **순수 상방(bonus)** |
| 목표치 보정 기준 | smart 기본 정답률(80%) 기준 | **WAGER 정답률 0% 기준으로 70% 클리어** |
| 근거 | 반복 플레이어 대상 | 1회 플레이 + 학습 대상자 |

**즉 부스판 목표치는 "확률을 하나도 못 맞혀도 70%가 클리어"하도록 잡는다.**
확률을 잘 맞히는 학생은 더 여유롭게 클리어할 뿐, 못 맞히는 학생이 탈락하지는 않는다.
이것이 교육용 게임과 실력 게임을 가르는 지점이다.

#### ✅ P2-B 측정 결과 — 현행 목표치가 이미 원칙을 만족한다

| tier (sum) | WAGER 0% | WAGER 50% | WAGER 100% |
|---|--:|--:|--:|
| greedy | **71.3%** | 85.0% | 87.2% |
| smart | 94.7% | 94.2% | 94.2% |

`[490, 630, 640]` 그대로 기준을 충족하므로 **재역산하지 않는다.**
부스판에서 WAGER는 71.3% → 87.2%의 **순수 상방**으로 작동한다.

대조군으로 풀버전은 smart 기준 4.2% → 40.4%(+36.2%p)다. 같은 장치가 버전에 따라
전혀 다른 역할을 하며, 그것이 이 절을 따로 둔 이유다.

---

## 13. 미해결 / 후속 결정

| # | 항목 | 상태 |
|---|---|---|
| 1 | 교과서 성취기준 매핑 | ✅ **완료** (1절, 2022 개정 교육과정 반영) |
| 2 | 목표치 곡선 | ✅ **확정** — `[490, 630, 640, 880, 1080, 1250, 1330, 1530]` (13-6절) |
| 3 | 배율 스택 방식 | ✅ **`sum` 확정** (smart 28.1% vs additive_delta 14.2%) |
| 4 | 부스판 목표치 | ✅ **`[490, 630, 640]` 확정** — greedy 85.3%, WAGER 0%에서도 71.3% |
| 5 | 사운드 · BGM | ⏳ Phase 6 |
| 6 | 팀 구성 | ✅ **1인 개발·운영 확정** |
| 7 | 몬티 홀 이벤트 도입 여부 (교과서 Ⅱ단원 수록 소재) | ⏳ Phase 6 |
| 8 | WAGER 문항 뱅크 작성 (조건부확률 표현 규칙 준수, 1-4절 ③) | ⏳ Phase 5 |
| 9 | 아트 제작 방식 | ✅ **프로시저럴 생성 확정** (11절) |
| 10 | 부스 노트북 대수 확보 | ⏳ 미정 — 3대 이상 권장 |
| 11 | 도형 중복에 의한 점수 폭발 | ✅ **해소** — 1.95배, 상한 규칙 불필요 |
| 12 | 떠돌이 조각 가격 | ✅ **무상 지급으로 전환** — 13-4절 개정 |
| 13 | `settle()`의 칩별 기여 분해 | ⏳ **P5 선행 조건** — DRIFT ORACLE의 "떠돌이 점수 +50%"에 필요 |
| 14 | 동반성 효과 반영 클리어율 재측정 | ⏳ **P4 이후 P2-B 재실행** |
| 15 | `Loadout.companions` 필수 전환 | ⏳ P4 |
| 16 | 배치 실력 의존도 | ⏳ **#20으로 이관** — 부스판은 조건 충족, 풀버전만 남음 |
| 17 | 학습 행동의 점수 기여도 | ✅ **역전** — 풀버전 WAGER 0%↔100%가 36.2%p 차이 (13-6절 ①) |
| 18 | 떠돌이가 필수품화 | ✅ **무상 지급으로 해소** — 13-6절 ② |
| 19 | 부스판 WAGER 민감도 | ✅ **측정 완료** — 0%에서 greedy 71.3% (12-5절 기준 충족) |
| 20 | 풀버전 실력 격차 (greedy 2.4% vs smart 28.1%) | ⏳ P3 이후 — 부스판은 조건 충족 |
| 21 | ⚠️ **실제 부스 참여자의 실력 위치 미상** | ⏳ random 10.7% ~ greedy 85.3% 사이 어디인지 불명 — 13-6절 ⑤ |
| 22 | иєвυℓα 디자인 · 대사 설계 | ✅ **P4-A 확정** — 11-9절 |
| 23 | `COMPANIONS_STOCKED` / `COMPANIONS_ENABLED` 분리 | ⏳ **P4-B에서 후자를 true로** — 아래 |
| 24 | иєвυℓα 스프라이트 2차 개정 | ⏳ **수동 작업** — 11-9절 "남은 개정" 4항목 |

#### 미해결 #23 — 동반성 두 플래그의 분리 (P4-A)

"진열되어 있다"와 "살 수 있다"는 서로 다른 질문이고, P4-A는 전자만 참이어야 한다.

| 플래그 | P4-A | 뜻 |
|---|---|---|
| `COMPANIONS_STOCKED` | **true** | 상점 진열. 7-1절의 티어 확률표는 공개가 교육 장치인데, 빈 칸으로는 아무것도 가르치지 못한다 |
| `COMPANIONS_ENABLED` | **false** | 구매 가능. 효과 파라미터가 생기는 **P4-B에서 true로 전환**한다 (7-1-b) |

> ★ **절대 조건**: `COMPANIONS_ENABLED`가 false인 동안 동반성은 **게임 상태에 어떤 경로로도 진입할 수 없다.**
> 13-6절의 목표 곡선은 전부 "동반성 없음" 기준으로 측정됐으므로, 한 개라도 새어 들어가면 그 수치가 무효가 된다.
>
> 런타임 가드는 잊힐 수 있으므로 **타입으로 막았다**: `Purchase` 유니온에 `companion` 변형이 없다.
> 동반성 구매는 값으로 표현조차 되지 않으므로 `applyPurchase`에 넘길 수 없고, 따라서
> `Loadout.companions`가 자랄 방법이 없다. P4-B는 이 변형을 추가하는 것에서 시작한다.

---

### 13-1. P2 시뮬레이터가 반드시 포함해야 할 케이스

**① 단일 문양 도배 보드 + 물병자리·처녀자리 동시 보유**
꼭짓점이 다르면 별개 라인이므로, 5×5를 한 문양으로 채우면
ㅅ자 약 12개 + ㅗ자 약 15개가 동시 발동할 수 있다. 각 라인이 `(칸수 × 10) × 배율`을 받으므로
점수가 폭발할 여지가 있다. 상한이 필요한지 수치로 확인한다.

> ✅ **해소** (`tests/gdd13-1.test.ts`): ㅅ자 12개 + ㅗ자 12개가 동시 발동해도
> 물병+처녀 2,432점 vs 궁수+사자 1,250점으로 **1.95배**에 그친다. 네 별자리 전부라도 3,662점.
> **상한 규칙을 만들지 않는다.** (미해결 #11)

**② 떠돌이 조각 + 특수 조각 3개 인접**
한 칸이 최대 5종 판정을 받는 최상위 시너지. 20 스타더스트 가격이 맞는지 확인한다.

> ✅ **해소** (`tests/gdd13-1.test.ts`): 칸 기여 최상 50 / 보통 30 / 최악 10점·턴.
> 가격 20 기준 스타더스트 1당 2.5 / 1.5 / 0.5점으로 기본 조각(3.3)보다 나빴다.
> → 최종적으로 **무상 지급으로 전환**됐다 (13-4절, 미해결 #12·#18).

### 13-2. P2-A에서 드러난 두 가지 설계 위험

**① 배치 실력 의존도**

무작위 배치 정책은 별자리를 4개 보유해도 **정확히 600점**에 고정된다 (40+80+120+160+200).
라인이 하나도 생기지 않으면 별자리는 아무 일도 하지 않기 때문이다.
그런데 8라운드 목표는 9,000점 — **15배 격차**다.

| 함의 | 내용 |
|---|---|
| 승패 결정 요인 | 확률 판단이 아니라 **배치 감각** |
| 부스 리스크 | 중학생이 20분에 배치 감각을 익히지 못하면 3라운드에서 전멸 |

> **여기의 20분은 총 체류 시간**이다 — 룰 설명과 좌석 교대를 포함한, 참여자가 부스에
> 머무는 시간. 12-1절 표의 "부스판 15분"은 **플레이 시간만** 센 값이라 서로 다른 값을
> 재는 것이고, 모순이 아니다. 실제 처리량 재산정은 P6에서 한다.
| 충돌하는 목표 | GDD 12-4절 "부스판 클리어율 70% 이상" |

**② 학습 행동의 점수 기여도**

| 학습 행동 | 현재 보상 | 라운드 점수 대비 |
|---|---|---|
| WAGER 적중 | 스타더스트 +2 | 무시 가능 |
| DRIFT ORACLE 정답 | 스타더스트 +1 | 무시 가능 |

**교육용 게임에서 학습 행동이 승패와 무관하면 설계가 실패한 것이다.**
학생은 확률 판단을 곁가지로 인식하고 건너뛰게 된다.

> 두 문제 모두 **P2-B의 수치를 본 뒤에** 처방한다.
> 지금 추측으로 숫자를 바꾸면 P2-B가 측정하는 대상이 흐려진다.

### 13-3. 🚨 목표 곡선 폐기 및 재설계 기준

#### 라운드 점수의 이론적 상한 (한 문양 도배 + 최적 별자리 4개 + 완벽 배치)

| 덱 구성 | sum | product | additive_delta |
|---|---|---|---|
| 기본 조각 전부 | **5,648** | 5,748 | 5,481 |
| 특수 조각 전부 (2문양 판정) | 11,296 | 11,496 | 10,962 |

#### 무엇이 잘못됐나

| 라운드 | 기존 목표 | 기본 조각 상한 대비 | 판정 |
|---|---|---|---|
| R7 | 5,500 | **97.4%** | 완벽 플레이로 겨우 도달 |
| R8 | 9,000 | **159%** | **규칙상 도달 불가** |

R8을 넘으려면 덱 20장이 전부 동일한 특수 조각이어야 하고, 그러고도 이론 상한의 80%를 뽑아야 한다.
기존 곡선은 "어렵다"가 아니라 **틀렸다.** 지수 성장(×1.62)을 상한 확인 없이 그린 설계 오류다.

#### 재설계 기준 (수치가 아니라 조건으로 정의한다)

새 `TARGET_SCORES`는 아래 3개를 **동시에** 만족해야 한다.

| # | 조건 | 근거 |
|---|---|---|
| 1 | smart의 풀버전 전체 클리어율 **20~30%** | GDD 10절 |
| 2 | greedy의 부스판 3라운드 클리어율 **70% 이상** | GDD 12-4절 |
| 3 | 모든 라운드 목표 ≤ 해당 라운드 **기계적 상한의 70%** | 완벽 플레이를 강요하지 않기 위함 |

수치는 시뮬레이션 분포에서 역산한다. 손으로 그리지 않는다.
조건 1과 2가 충돌하면(부스판은 R1~R3를 공유한다) **조건 2를 우선**한다.
부스판 실패는 곧 투표 손실이기 때문이다.

---

### 13-4. 떠돌이 조각 가격 — 교육적 근거로 인하

측정 결과 스타더스트 1당 기여가 최상 2.5 / 보통 1.5 / 최악 0.5점·턴으로,
기본 조각(3.3)보다 **나쁘다.** 가격 20은 순수 효율로는 명백한 과다다.

그런데 여기엔 점수 효율보다 중요한 이유가 있다.

> **떠돌이 조각을 사지 않으면 `DRIFT ORACLE`(GDD 8-3)이 게임 내내 한 번도 발동하지 않는다.**
> 이 장치는 교과서 Ⅲ-2 **기댓값**을 가르치는 유일한 수단이며,
> 4개 학습 장치 중 확률 개념이 가장 직접적으로 드러나는 순간이다.

즉 이 아이템의 가격은 **밸런스 문제가 아니라 학습 노출률 문제**다.
학생이 "비싸서 안 삼" → 기댓값 단원이 통째로 사라진다.

#### P2-B 결과: 인하는 과잉 교정이었다

| 지표 | 가격 20 | 가격 10 |
|---|---|---|
| 구매율 | 28.3% | **98.6%** |
| 미구매 시 클리어율 | — | **0.0%** (전 등급) |

학습 노출률 목표는 달성했으나, **선택이 아니라 세금**이 됐다.
98%가 첫 상점에서 즉시 구매하므로 "게임당 1회 한정"이라는 희소성 연출도 무의미하다.
더 심각한 것은, 이해 못 하고 안 산 학생은 **그 시점에 이미 게임이 끝난다**는 점이다.

#### 확정: 구매가 아니라 무상 지급

**иєвυℓα와의 첫 만남에서 떠돌이 조각을 무상으로 지급**한다.

| 효과 | 내용 |
|---|---|
| DRIFT ORACLE 발동률 | 98.6% → **100%** |
| 가짜 선택지 제거 | 스타더스트가 실제 의사결정에 쓰인다 |
| 목표 곡선 유효성 | 시뮬 전제(98.6% 보유)와 사실상 동일 → **재보정 불필요** |
| 서사 | 딜러가 첫 거래 기념으로 건네는 선물 |

`SHOP_PRICES`의 떠돌이 항목을 제거하고, 상점 진열에서도 뺀다.

> **P4-A 보강**: `SHOP_SLOTS.drifterChips`가 남아 있던 것을 함께 제거했다.
> `rollStock`이 읽지 않아 무해했지만, 죽은 설정값은 나중에 되살아나 이 절을 조용히 깨뜨린다.
> 떠돌이는 **재고가 아니라 선물**이므로 진열 칸이라는 개념 자체가 없어야 한다.
> 9-3의 상점 구성 박스에서도 떠돌이 줄을 뺐다 — 지급은 첫 방문 시 `grantDrifter`가 한다.

---

### 13-5. 🚨 R1은 플레이어 행동과 무관하다

R1 점수는 **모든 등급에서 정확히 600점, 표준편차 0**이다.
별자리는 R1 클리어 후 상점에서만 살 수 있는데, 별자리가 없으면 라인이 발동하지 않아
점유 칸수 × 10의 합(40+80+120+160+200)만 남기 때문이다.

| 문제 | 영향 |
|---|---|
| 실력·확률 판단이 점수에 **0** 반영 | 첫 5턴 동안 피드백 루프가 없다 |
| 첫인상 구간 | 부스 참여자가 게임을 판단하는 바로 그 5턴 |
| 별자리 개념 미노출 | 게임의 핵심 시스템을 R2에야 처음 본다 |

#### 처방: 시작 별자리 1개 지급

게임 시작 시 **양자리(세로 3연속, ×1.2) / 천칭자리(가로 3연속, ×1.2) 중 1개를 선택**하게 한다.

- 두 별자리는 배율이 같고 축만 다르다 → 밸런스 영향 없이 **"세로냐 가로냐"** 라는 첫 의사결정이 생긴다
- 별자리 시스템을 R1부터 체험한다
- R1 점수에 변동성이 생겨 배치 실력이 즉시 피드백된다
- 보유 한도 4개 중 1칸을 채우고 시작한다

> 이 변경은 R1 목표치 재산정을 동반한다. 13-3절 기준에 함께 반영한다.

---

### 13-6. P2-B 최종 측정 (조합당 1,000회 · 시드 20260101 · 동반성 비활성)

전문은 `sim/out/results.md`. P2-A 규칙 기준선은 `sim/out/results-p2a-baseline.md`에 보존했다.
아래는 모두 **떠돌이 무상 지급(13-4절) + 시작 별자리(13-5절)** 를 반영한 최종 규칙 기준이다.

#### 조건 판정

| # | 조건 | 측정값 | 판정 |
|---|---|---|---|
| 1 | smart 풀버전 20~30% | **28.1%** | ✅ |
| 2 | greedy 부스판 ≥70% | **85.3%** | ✅ |
| 3 | 전 라운드 ≤ 기계적 상한 70% | 최대 68.6% (R1) | ✅ |
| 12-5 | **WAGER 0%에서도 greedy 부스판 ≥70%** | **71.3%** | ✅ |

#### ① 부스판 WAGER 민감도 — 원칙이 지켜졌다 (미해결 #19 해소)

| tier (sum) | WAGER 0% | WAGER 50% | WAGER 100% | 0%→100% |
|---|--:|--:|--:|--:|
| greedy | **71.3%** | 85.0% | 87.2% | +15.9%p |
| smart | 94.7% | 94.2% | 94.2% | −0.5%p |

**부스판에서 WAGER는 순수 상방이다.** 확률을 하나도 못 맞혀도 greedy는 71.3%가 클리어하고,
잘 맞히면 87.2%로 올라간다. 목표치 재역산은 불필요하다.

smart가 −0.5%p로 평평한 것은 오차 범위다 — smart는 WAGER 없이도 부스판을 94% 클리어하므로
스타더스트가 더 들어와도 더 넘을 것이 없다.

풀버전은 정반대다. smart 기준 4.2% → 40.4%(**+36.2%p**)로, WAGER가 승패를 지배한다.
**두 버전에 서로 다른 원칙을 적용한 12-5절의 판단이 수치로 정당화됐다.**

#### ② 떠돌이 무상 지급의 실제 효과 (미해결 #18 해소)

| 지표 (sum) | 가격 10 | 무상 지급 |
|---|--:|--:|
| greedy 부스판 클리어율 | 72.3% | **85.3%** |
| smart 풀버전 클리어율 | 22.0% | **28.1%** |
| 떠돌이 칸 기여 (greedy) | 53.3점/턴 | **66.5점/턴** |
| 떠돌이 칸 기여 (smart) | 81.2점/턴 | **103.6점/턴** |

> ⚠️ **"재보정 불필요"는 절반만 맞았다.** 보유율은 98.6% → 100%로 거의 그대로지만,
> **10 스타더스트가 통째로 다른 데 쓰이게 됐다.** 그건 별자리 하나 값이다.
> 그 결과 별자리를 하나 더 사고 → 라인이 늘고 → 떠돌이 칸 기여까지 25% 올라갔다.

그래도 세 조건은 모두 유지된다. 다만 **smart 풀버전 28.1%는 20~30% 구간의 상단**이다.
P4에서 동반성이 들어오면 30%를 넘길 가능성이 높다 → 미해결 #14(P4 이후 재측정)에서 처리한다.

#### ③ 시작 별자리는 여전히 밸런스의 전제다

| (sum) | 시작 별자리 없음 | 있음 |
|---|--:|--:|
| greedy 부스판 | 62.1% | **85.3%** |
| smart 풀버전 | 18.8% | **28.1%** |
| R1 표준편차 (greedy) | **0** | **28** |

#### ④ 배율 스택 — `sum` 확정 유지

smart 풀버전: `sum` 28.1% · `product` 25.2% · `additive_delta` 14.2%.
부스판은 세 방식이 사실상 동일(85.1~85.3%)하다 — R1~R3에서는 한 라인에 별자리가 둘 이상 걸리는 일이 드물다.

#### ⑤ 미해결 #20 — 풀버전 실력 격차는 그대로

| tier | 풀버전 | 부스판 |
|---|--:|--:|
| random | 0.0% | **10.7%** |
| greedy | **2.4%** | 85.3% |
| smart | 28.1% | 94.1% |

무상 지급으로 greedy 풀버전이 1.3% → 2.4%로 올랐지만 여전히 사실상 클리어 불가다.
부스판이 우선이고 부스판은 조건을 충족하므로 P3 이후로 미룬다(#20).

> ⚠️ **random 부스판 10.7%는 별도로 볼 값이다.** `random`은 빈 칸을 균일 무작위로 고르는
> 정책이라 실제 참여자의 **하한**이고, `greedy`(매 턴 정산 최대화)는 **상한**이다.
> 룰 설명을 듣고 20분 플레이하는 중학생은 그 사이 어딘가이며,
> 현재 데이터로는 10.7%와 85.3% 중 어디에 가까운지 특정할 수 없다. → 미해결 #21

---

